# 🚀 下一步工作计划 - 领域项目管理全量实施

> **计划日期**: 2026-01-19  
> **预计周期**: 6周（30个工作日）  
> **目标产出**: C0领域项目管理完整功能上线  
> **设计依据**: domain-proj-workspace/领域项目管理-V3新方案.md

---

## 📋 工作总览

### 实施策略：渐进式开发 ⭐⭐⭐⭐⭐

```
Week 1-2: 核心组件开发
  ↓
Week 3-4: 核心页面开发
  ↓
Week 5: 功能完善与集成
  ↓
Week 6: 测试与优化
  ↓
MVP发布！
```

---

## Week 1: 核心组件开发（基础层）

### Day 1-2: IterationAxis 组件 ⭐⭐⭐⭐

**目标**: 开发统一迭代轴组件（项目的基础时间轴）

**交付物**:
```
frontend/src/components/project/IterationAxis.vue
frontend/src/components/project/IterationCell.vue
```

**核心功能**:
1. ✅ 显示26个迭代格子
2. ✅ 周/月视图切换
3. ✅ 里程碑标记显示
4. ✅ 当前迭代高亮
5. ✅ 响应式布局

**技术要点**:
```vue
<template>
  <div class="iteration-axis">
    <!-- 视图切换 -->
    <div class="view-switcher">
      <el-radio-group v-model="viewMode">
        <el-radio-button label="week">周视图</el-radio-button>
        <el-radio-button label="month">月视图</el-radio-button>
      </el-radio-group>
    </div>
    
    <!-- 迭代格子 -->
    <div class="iteration-cells" :class="`view-${viewMode}`">
      <IterationCell
        v-for="iteration in iterations"
        :key="iteration.id"
        :iteration="iteration"
        :is-current="iteration.id === currentIterationId"
        :milestone="getMilestoneForIteration(iteration)"
      />
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import { Iteration, Milestone } from '@/types/project'
import IterationCell from './IterationCell.vue'

interface Props {
  iterations: Iteration[]
  milestones?: Milestone[]
  currentIterationId?: string
}

const props = defineProps<Props>()
const viewMode = ref<'week' | 'month'>('week')

function getMilestoneForIteration(iteration: Iteration): Milestone | undefined {
  // 查找对齐到该迭代的里程碑
  return props.milestones?.find(m => {
    const milestoneDate = new Date(m.targetDate)
    const iterStart = new Date(iteration.startDate)
    const iterEnd = new Date(iteration.endDate)
    return milestoneDate >= iterStart && milestoneDate <= iterEnd
  })
}
</script>
```

**测试用例**:
1. 验证26个迭代全部显示
2. 验证视图切换功能
3. 验证里程碑标记正确显示
4. 验证当前迭代高亮

---

### Day 3-5: VersionGantt 组件 ⭐⭐⭐⭐⭐（最核心）

**目标**: 开发版本甘特图组件（多产品版本规划工作台的核心）

**交付物**:
```
frontend/src/components/version/VersionGantt.vue
frontend/src/components/version/VersionBar.vue
frontend/src/components/version/MilestoneLine.vue
```

**核心功能**:
1. ✅ 迭代轴展示
2. ✅ 版本条拖拽创建 ⭐⭐⭐⭐⭐
3. ✅ 版本条拖拽调整（起止时间）
4. ✅ 完成度显示（0-100%）
5. ✅ 里程碑对齐线显示
6. ✅ PI高亮显示

**技术要点**:
```vue
<template>
  <div class="version-gantt">
    <!-- 迭代轴 -->
    <IterationAxis 
      :iterations="iterations" 
      :milestones="milestones"
      :current-iteration-id="currentIterationId"
    />
    
    <!-- 产品分组 -->
    <div class="product-groups">
      <div 
        v-for="product in products" 
        :key="product.id"
        class="product-group"
      >
        <div class="product-header">
          {{ product.name }}
        </div>
        
        <!-- 版本条（可拖拽） -->
        <div 
          class="version-track"
          @mousedown="handleTrackMouseDown($event, product)"
        >
          <VersionBar
            v-for="version in getVersionsByProduct(product.id)"
            :key="version.id"
            :version="version"
            :iterations="iterations"
            :is-dragging="draggingVersion?.id === version.id"
            @drag-start="handleVersionDragStart"
            @drag-move="handleVersionDragMove"
            @drag-end="handleVersionDragEnd"
            @click="handleVersionClick"
          />
        </div>
      </div>
    </div>
    
    <!-- 里程碑对齐线 -->
    <MilestoneLine
      v-for="milestone in milestones"
      :key="milestone.id"
      :milestone="milestone"
      :iterations="iterations"
      :versions="getVersionsAlignedToMilestone(milestone)"
    />
    
    <!-- PI高亮区域 -->
    <div 
      v-for="pi in pis" 
      :key="pi.id"
      class="pi-highlight"
      :style="getPIStyle(pi)"
    >
      {{ pi.name }}
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import { Version, Iteration, Milestone, PI, Product } from '@/types/project'
import IterationAxis from '@/components/project/IterationAxis.vue'
import VersionBar from './VersionBar.vue'
import MilestoneLine from './MilestoneLine.vue'

interface Props {
  iterations: Iteration[]
  versions: Version[]
  milestones?: Milestone[]
  pis?: PI[]
  products: Product[]
  currentIterationId?: string
}

const props = defineProps<Props>()
const emit = defineEmits<{
  versionCreated: [version: Version]
  versionUpdated: [version: Version]
  versionDeleted: [versionId: string]
}>()

const draggingVersion = ref<Version | null>(null)
const creatingVersion = ref<{product: Product; startX: number} | null>(null)

// 拖拽创建新版本
function handleTrackMouseDown(event: MouseEvent, product: Product) {
  const rect = (event.target as HTMLElement).getBoundingClientRect()
  creatingVersion.value = {
    product,
    startX: event.clientX - rect.left
  }
  
  document.addEventListener('mousemove', handleCreatingMouseMove)
  document.addEventListener('mouseup', handleCreatingMouseUp)
}

function handleCreatingMouseMove(event: MouseEvent) {
  // 实时显示创建预览
}

function handleCreatingMouseUp(event: MouseEvent) {
  if (!creatingVersion.value) return
  
  // 计算起止迭代
  const startIteration = calculateIterationFromX(creatingVersion.value.startX)
  const endIteration = calculateIterationFromX(event.clientX)
  
  // 创建新版本
  const newVersion: Version = {
    id: `version-${Date.now()}`,
    productId: creatingVersion.value.product.id,
    versionNumber: '1.0',
    startIterationId: startIteration.id,
    endIterationId: endIteration.id,
    completion: 0
  }
  
  emit('versionCreated', newVersion)
  
  creatingVersion.value = null
  document.removeEventListener('mousemove', handleCreatingMouseMove)
  document.removeEventListener('mouseup', handleCreatingMouseUp)
}

// 拖拽调整版本
function handleVersionDragStart(version: Version) {
  draggingVersion.value = version
}

function handleVersionDragMove(version: Version, deltaX: number) {
  // 计算新的起止迭代
  // 实时更新版本位置
}

function handleVersionDragEnd(version: Version) {
  emit('versionUpdated', version)
  draggingVersion.value = null
}

function handleVersionClick(version: Version) {
  // 打开版本详情
}

function getVersionsByProduct(productId: string): Version[] {
  return props.versions.filter(v => v.productId === productId)
}

function getVersionsAlignedToMilestone(milestone: Milestone): Version[] {
  // 查找对齐到该里程碑的版本
  return props.versions.filter(v => v.alignedMilestoneId === milestone.id)
}

function getPIStyle(pi: PI) {
  // 计算PI在甘特图中的位置和宽度
  const startIter = props.iterations.find(i => i.id === pi.startIterationId)
  const endIter = props.iterations.find(i => i.id === pi.endIterationId)
  // ...
}

function calculateIterationFromX(x: number): Iteration {
  // 根据鼠标X坐标计算对应的迭代
  // ...
}
</script>

<style scoped>
.version-gantt {
  position: relative;
  min-height: 600px;
}

.product-groups {
  margin-top: 20px;
}

.product-group {
  display: flex;
  margin-bottom: 8px;
  border-bottom: 1px solid #e4e7ed;
}

.product-header {
  width: 150px;
  padding: 12px;
  font-weight: 600;
  background: #f5f7fa;
}

.version-track {
  flex: 1;
  position: relative;
  min-height: 60px;
  cursor: crosshair;
}

.version-track:hover {
  background: #f9fafb;
}

.pi-highlight {
  position: absolute;
  background: rgba(64, 158, 255, 0.1);
  border: 2px dashed #409eff;
  pointer-events: none;
}
</style>
```

**复杂度**:
- 🔴 **高复杂度**：拖拽交互、坐标计算、实时预览
- ⏱️ **预计3天**：Day 3完成基础布局，Day 4完成拖拽创建，Day 5完成拖拽调整

**测试用例**:
1. 拖拽创建版本条
2. 拖拽调整版本起止时间
3. 完成度显示正确
4. 里程碑对齐线位置正确
5. PI高亮区域显示正确

---

### Day 6-7: ProjectWizard 组件（Step 1-3）⭐⭐⭐⭐

**目标**: 开发项目创建5步向导的前3步

**交付物**:
```
frontend/src/components/project/ProjectWizard.vue
frontend/src/components/project/wizard/Step1BasicInfo.vue
frontend/src/components/project/wizard/Step2TimeConfig.vue
frontend/src/components/project/wizard/Step3TeamConfig.vue
```

**Step 1: 基本信息**
```vue
<template>
  <el-form :model="form" :rules="rules" ref="formRef">
    <el-form-item label="项目名称" prop="name">
      <el-input 
        v-model="form.name" 
        placeholder="例如：岚图H56整车软件研发项目"
      />
    </el-form-item>
    
    <el-form-item label="项目代号" prop="code">
      <el-input 
        v-model="form.code" 
        placeholder="例如：H56-SW"
      />
    </el-form-item>
    
    <el-form-item label="项目描述" prop="description">
      <el-input 
        v-model="form.description" 
        type="textarea" 
        :rows="4"
      />
    </el-form-item>
    
    <el-form-item label="项目负责人" prop="ownerId">
      <el-select v-model="form.ownerId" filterable>
        <el-option 
          v-for="user in users" 
          :key="user.id"
          :label="user.name" 
          :value="user.id"
        />
      </el-select>
    </el-form-item>
  </el-form>
</template>
```

**Step 2: 时间配置**
```vue
<template>
  <el-form :model="form" :rules="rules" ref="formRef">
    <el-form-item label="项目开始日期" prop="startDate">
      <el-date-picker 
        v-model="form.startDate" 
        type="date"
        :picker-options="startDateOptions"
      />
    </el-form-item>
    
    <el-form-item label="迭代周期" prop="iterationWeeks">
      <el-radio-group v-model="form.iterationWeeks">
        <el-radio :label="2">2周/迭代</el-radio>
        <el-radio :label="3">3周/迭代</el-radio>
      </el-radio-group>
    </el-form-item>
    
    <el-form-item label="总迭代数" prop="totalIterations">
      <el-input-number 
        v-model="form.totalIterations" 
        :min="12" 
        :max="36"
      />
    </el-form-item>
    
    <!-- 迭代预览 -->
    <el-alert type="info" :closable="false" style="margin-top: 16px;">
      <template #title>
        项目结束日期：{{ projectEndDate }}
      </template>
    </el-alert>
    
    <IterationAxis 
      :iterations="previewIterations" 
      style="margin-top: 16px;"
    />
  </el-form>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import IterationAxis from '@/components/project/IterationAxis.vue'
import { generateIterations } from '@/utils/iterationUtils'

const previewIterations = computed(() => {
  return generateIterations(
    form.startDate,
    form.iterationWeeks,
    form.totalIterations
  )
})

const projectEndDate = computed(() => {
  if (previewIterations.value.length === 0) return ''
  const lastIter = previewIterations.value[previewIterations.value.length - 1]
  return formatDate(lastIter.endDate)
})
</script>
```

**Step 3: 团队配置**
```vue
<template>
  <el-form :model="form" ref="formRef">
    <el-form-item label="选择团队">
      <el-transfer
        v-model="form.teamIds"
        :data="availableTeams"
        :titles="['可选团队', '已选团队']"
      />
    </el-form-item>
    
    <!-- 团队详情 -->
    <div v-for="teamId in form.teamIds" :key="teamId">
      <el-card style="margin-bottom: 16px;">
        <template #header>
          {{ getTeamName(teamId) }}
        </template>
        <el-descriptions :column="2">
          <el-descriptions-item label="成员数">
            {{ getTeamMemberCount(teamId) }}
          </el-descriptions-item>
          <el-descriptions-item label="技术栈">
            {{ getTeamTechStack(teamId) }}
          </el-descriptions-item>
          <el-descriptions-item label="容量/迭代">
            {{ getTeamCapacity(teamId) }} SP
          </el-descriptions-item>
        </el-descriptions>
      </el-card>
    </div>
  </el-form>
</template>
```

**工作量**: 2天

---

## Week 2: 核心组件开发（续）+ 开始页面开发

### Day 8-10: ProjectWizard 组件（Step 4-5 + PI生成）⭐⭐⭐⭐⭐

**Step 4: 里程碑配置**
```vue
<template>
  <el-form :model="form" ref="formRef">
    <div style="margin-bottom: 16px;">
      <el-button type="primary" @click="addMilestone">
        <el-icon><Plus /></el-icon>
        添加里程碑
      </el-button>
    </div>
    
    <el-table :data="form.milestones">
      <el-table-column label="里程碑名称">
        <template #default="{ row }">
          <el-input v-model="row.name" />
        </template>
      </el-table-column>
      
      <el-table-column label="类型">
        <template #default="{ row }">
          <el-select v-model="row.type">
            <el-option label="工程样车(EP)" value="ep" />
            <el-option label="PP车(PP)" value="pp" />
            <el-option label="量产车(SOP)" value="sop" />
            <!-- ... 其他类型 -->
          </el-select>
        </template>
      </el-table-column>
      
      <el-table-column label="目标日期">
        <template #default="{ row }">
          <el-date-picker v-model="row.targetDate" type="date" />
        </template>
      </el-table-column>
      
      <el-table-column label="对齐迭代">
        <template #default="{ row }">
          <el-tag>{{ getAlignedIteration(row.targetDate) }}</el-tag>
        </template>
      </el-table-column>
      
      <el-table-column label="操作">
        <template #default="{ $index }">
          <el-button 
            text 
            type="danger" 
            @click="removeMilestone($index)"
          >
            删除
          </el-button>
        </template>
      </el-table-column>
    </el-table>
    
    <!-- 里程碑在迭代轴上的可视化 -->
    <div style="margin-top: 24px;">
      <el-text tag="b">里程碑预览</el-text>
      <IterationAxis 
        :iterations="previewIterations" 
        :milestones="form.milestones"
        style="margin-top: 12px;"
      />
    </div>
  </el-form>
</template>
```

**Step 5: 确认创建（自动生成PI）⭐⭐⭐⭐⭐**
```vue
<template>
  <div>
    <el-descriptions :column="2" border>
      <el-descriptions-item label="项目名称">{{ form.name }}</el-descriptions-item>
      <el-descriptions-item label="项目代号">{{ form.code }}</el-descriptions-item>
      <el-descriptions-item label="开始日期">{{ formatDate(form.startDate) }}</el-descriptions-item>
      <el-descriptions-item label="迭代周期">{{ form.iterationWeeks }}周</el-descriptions-item>
      <el-descriptions-item label="总迭代数">{{ form.totalIterations }}</el-descriptions-item>
      <el-descriptions-item label="团队数">{{ form.teamIds.length }}</el-descriptions-item>
      <el-descriptions-item label="里程碑数">{{ form.milestones.length }}</el-descriptions-item>
    </el-descriptions>
    
    <el-divider />
    
    <el-alert type="success" :closable="false" style="margin-bottom: 16px;">
      <template #title>
        🎉 系统将自动创建{{ generatedPIs.length }}个PI，并生成{{ totalSprintsCount }}个Sprint
      </template>
    </el-alert>
    
    <!-- PI预览 -->
    <div v-for="pi in generatedPIs" :key="pi.name">
      <el-card style="margin-bottom: 16px;">
        <template #header>
          <div style="display: flex; justify-content: space-between;">
            <span>{{ pi.name }}</span>
            <el-tag>{{ pi.sprints.length }} Sprints</el-tag>
          </div>
        </template>
        
        <el-descriptions :column="2">
          <el-descriptions-item label="迭代范围">
            迭代{{ pi.startIterationNumber }} ~ 迭代{{ pi.endIterationNumber }}
          </el-descriptions-item>
          <el-descriptions-item label="时间范围">
            {{ formatDate(pi.startDate) }} ~ {{ formatDate(pi.endDate) }}
          </el-descriptions-item>
          <el-descriptions-item label="对齐里程碑">
            <el-tag type="success">{{ pi.milestone.name }}</el-tag>
          </el-descriptions-item>
          <el-descriptions-item label="Sprint列表">
            <el-space wrap>
              <el-tag 
                v-for="sprint in pi.sprints" 
                :key="sprint.name"
                size="small"
              >
                {{ sprint.name }}
              </el-tag>
            </el-space>
          </el-descriptions-item>
        </el-descriptions>
      </el-card>
    </div>
    
    <el-alert type="warning" :closable="false">
      <template #title>
        ⚠️ 创建后，可在"版本规划工作台"中添加产品版本，并为每个PI规划具体Feature
      </template>
    </el-alert>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { generatePIsFromMilestones } from '@/utils/piUtils'

const generatedPIs = computed(() => {
  return generatePIsFromMilestones(
    previewIterations.value,
    form.milestones
  )
})

const totalSprintsCount = computed(() => {
  return generatedPIs.value.reduce((sum, pi) => sum + pi.sprints.length, 0)
})
</script>
```

**核心算法: PI自动生成**
```typescript
// frontend/src/utils/piUtils.ts

export function generatePIsFromMilestones(
  iterations: Iteration[],
  milestones: Milestone[]
): GeneratedPI[] {
  const pis: GeneratedPI[] = []
  
  // 按时间排序里程碑
  const sortedMilestones = [...milestones].sort(
    (a, b) => new Date(a.targetDate).getTime() - new Date(b.targetDate).getTime()
  )
  
  sortedMilestones.forEach((milestone, index) => {
    // 找到对齐的迭代
    const alignedIteration = findAlignedIteration(iterations, milestone.targetDate)
    if (!alignedIteration) return
    
    // 计算PI的起止迭代
    let startIteration: Iteration
    let endIteration: Iteration = alignedIteration
    
    if (index === 0) {
      // 第一个PI从第1个迭代开始
      startIteration = iterations[0]
    } else {
      // 从上一个PI的结束迭代的下一个迭代开始
      const prevPI = pis[pis.length - 1]
      const prevEndIndex = iterations.findIndex(i => i.id === prevPI.endIterationId)
      startIteration = iterations[prevEndIndex + 1]
    }
    
    // 生成Sprints（每个PI通常6个Sprint）
    const sprintCount = 6
    const iterationsInPI = iterations.filter(i => 
      i.number >= startIteration.number && i.number <= endIteration.number
    )
    
    const sprints = iterationsInPI.map((iter, idx) => ({
      id: `sprint-${iter.id}`,
      name: `Sprint ${startIteration.number + idx}`,
      piId: `pi-${milestone.id}`,
      iterationId: iter.id,
      startDate: iter.startDate,
      endDate: iter.endDate
    }))
    
    pis.push({
      id: `pi-${milestone.id}`,
      name: `${milestone.name} PI`,
      startIterationId: startIteration.id,
      endIterationId: endIteration.id,
      startIterationNumber: startIteration.number,
      endIterationNumber: endIteration.number,
      startDate: startIteration.startDate,
      endDate: endIteration.endDate,
      milestone,
      sprints
    })
  })
  
  return pis
}

function findAlignedIteration(
  iterations: Iteration[], 
  targetDate: string
): Iteration | undefined {
  const target = new Date(targetDate)
  
  // 查找目标日期所在的迭代
  let closestIter = iterations[0]
  let minDiff = Math.abs(new Date(closestIter.endDate).getTime() - target.getTime())
  
  for (const iter of iterations) {
    const diff = Math.abs(new Date(iter.endDate).getTime() - target.getTime())
    if (diff < minDiff) {
      minDiff = diff
      closestIter = iter
    }
  }
  
  // 如果差距在±7天内，认为对齐
  const SEVEN_DAYS = 7 * 24 * 60 * 60 * 1000
  return minDiff <= SEVEN_DAYS ? closestIter : undefined
}
```

**工作量**: 3天

---

## Week 3: 核心页面开发

### Day 11-15: VersionPlanning 工作台 ⭐⭐⭐⭐⭐（最核心页面）

**目标**: 开发多产品版本规划工作台（整个系统的核心页面）

**交付物**:
```
frontend/src/views/C0-Project/VersionPlanning.vue
frontend/src/components/version/VersionWizard.vue （版本创建对话框）
frontend/src/components/version/VersionDetail.vue （版本详情）
frontend/src/components/version/VersionCompletionEditor.vue （完成度编辑）
```

**页面结构**（参考前面的完整代码示例）:
1. 顶部统计面板
2. Timeline甘特图（VersionGantt组件）
3. 操作按钮区
4. 版本详情抽屉

**核心功能**:
- ✅ Timeline甘特图展示
- ✅ 拖拽创建版本条
- ✅ 拖拽调整版本时间
- ✅ 点击版本查看详情
- ✅ 编辑版本信息
- ✅ 设置版本完成度
- ✅ 分配Epic到版本
- ✅ 生成PI集合按钮
- ✅ 进入PI Planning入口

**完成度计算逻辑**:
```typescript
// frontend/src/stores/modules/version.ts

export const useVersionStore = defineStore('version', () => {
  const versions = ref<Version[]>([])
  const epicStore = useEpicStore()
  const featureStore = useFeatureStore()
  const sstsStore = useSSTSStore()
  
  // 计算版本完成度
  function calculateVersionCompletion(versionId: string): number {
    const version = versions.value.find(v => v.id === versionId)
    if (!version) return 0
    
    // 获取该版本关联的所有Epic
    const epics = epicStore.getEpicsByVersion(versionId)
    if (epics.length === 0) return 0
    
    let totalWeight = 0
    let weightedCompletion = 0
    
    epics.forEach(epic => {
      const epicWeight = epic.storyPoints || 1
      totalWeight += epicWeight
      
      // 计算Epic完成度
      const epicCompletion = calculateEpicCompletion(epic.id)
      weightedCompletion += epicCompletion * epicWeight
    })
    
    return totalWeight > 0 ? Math.round(weightedCompletion / totalWeight) : 0
  }
  
  function calculateEpicCompletion(epicId: string): number {
    const features = featureStore.getFeaturesByEpic(epicId)
    if (features.length === 0) return 0
    
    let totalWeight = 0
    let weightedCompletion = 0
    
    features.forEach(feature => {
      const featureWeight = feature.storyPoints || 1
      totalWeight += featureWeight
      
      const featureCompletion = calculateFeatureCompletion(feature.id)
      weightedCompletion += featureCompletion * featureWeight
    })
    
    return totalWeight > 0 ? weightedCompletion / totalWeight : 0
  }
  
  function calculateFeatureCompletion(featureId: string): number {
    const sstss = sstsStore.getSSTSsByFeature(featureId)
    if (sstss.length === 0) return 0
    
    let totalWeight = 0
    let weightedCompletion = 0
    
    sstss.forEach(ssts => {
      const sstsWeight = ssts.storyPoints || 1
      totalWeight += sstsWeight
      
      // SSTS完成度基于状态
      const sstsCompletion = getSSTSCompletion(ssts.status)
      weightedCompletion += sstsCompletion * sstsWeight
    })
    
    return totalWeight > 0 ? weightedCompletion / totalWeight : 0
  }
  
  function getSSTSCompletion(status: string): number {
    const statusMap: Record<string, number> = {
      'todo': 0,
      'in-progress': 50,
      'review': 80,
      'done': 100
    }
    return statusMap[status] || 0
  }
  
  // 监听SSTS状态变化，自动更新版本完成度
  watch(() => sstsStore.sstsList, () => {
    versions.value.forEach(version => {
      version.completion = calculateVersionCompletion(version.id)
    })
  }, { deep: true })
  
  return {
    versions,
    calculateVersionCompletion
  }
})
```

**工作量**: 5天（最复杂页面）

---

### Day 16-18: ProjectDetail/Timeline 页面 ⭐⭐⭐⭐

**目标**: 开发项目详情页面（包含Dashboard和时间线视图）

**交付物**:
```
frontend/src/views/C0-Project/ProjectDetail.vue
frontend/src/views/C0-Project/ProjectTimeline.vue
frontend/src/components/project/ProjectDashboard.vue
frontend/src/components/project/ProjectStats.vue
```

**页面结构**:
```vue
<template>
  <div class="project-detail">
    <!-- 顶部：面包屑 + 操作按钮 -->
    <div class="page-header">
      <el-breadcrumb>
        <el-breadcrumb-item :to="{ path: '/projects' }">项目列表</el-breadcrumb-item>
        <el-breadcrumb-item>{{ project.name }}</el-breadcrumb-item>
      </el-breadcrumb>
      
      <div class="actions">
        <el-button @click="gotoVersionPlanning">版本规划</el-button>
        <el-button @click="gotoPIPlanning">PI Planning</el-button>
      </div>
    </div>
    
    <!-- Tab切换 -->
    <el-tabs v-model="activeTab">
      <el-tab-pane label="Dashboard" name="dashboard">
        <ProjectDashboard :project="project" />
      </el-tab-pane>
      
      <el-tab-pane label="时间线" name="timeline">
        <ProjectTimeline :project="project" />
      </el-tab-pane>
      
      <el-tab-pane label="版本列表" name="versions">
        <!-- 版本列表 -->
      </el-tab-pane>
      
      <el-tab-pane label="PI列表" name="pis">
        <!-- PI列表 -->
      </el-tab-pane>
      
      <el-tab-pane label="团队" name="teams">
        <!-- 团队列表 -->
      </el-tab-pane>
    </el-tabs>
  </div>
</template>
```

**ProjectDashboard组件**:
```vue
<template>
  <div class="project-dashboard">
    <!-- 统计卡片 -->
    <el-row :gutter="16">
      <el-col :span="6">
        <el-card>
          <el-statistic title="总迭代数" :value="project.totalIterations" />
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <el-statistic title="产品版本" :value="versionsCount" />
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <el-statistic title="PI数量" :value="pisCount" />
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <el-statistic 
            title="整体进度" 
            :value="overallProgress" 
            suffix="%"
          />
        </el-card>
      </el-col>
    </el-row>
    
    <!-- 进度图表 -->
    <el-row :gutter="16" style="margin-top: 16px;">
      <el-col :span="12">
        <el-card title="版本进度">
          <ProgressChart :versions="versions" />
        </el-card>
      </el-col>
      <el-col :span="12">
        <el-card title="里程碑进度">
          <MilestoneChart :milestones="milestones" />
        </el-card>
      </el-col>
    </el-row>
    
    <!-- 风险提示 -->
    <el-card style="margin-top: 16px;">
      <template #header>风险提示</template>
      <el-alert 
        v-for="risk in risks" 
        :key="risk.id"
        :type="risk.level"
        :title="risk.title"
        :description="risk.description"
        style="margin-bottom: 8px;"
      />
    </el-card>
  </div>
</template>
```

**ProjectTimeline组件**（使用VersionGantt）:
```vue
<template>
  <div class="project-timeline">
    <VersionGantt
      :iterations="iterations"
      :versions="versions"
      :milestones="milestones"
      :pis="pis"
      :products="products"
      :current-iteration-id="currentIterationId"
      readonly
    />
  </div>
</template>
```

**工作量**: 3天

---

### Day 19-20: PIPlanning 页面 ⭐⭐⭐⭐

**目标**: 开发PI规划页面（里程碑对齐、PI列表）

**交付物**:
```
frontend/src/views/C0-Project/PIPlanning.vue
frontend/src/components/pi/PICard.vue
frontend/src/components/pi/PIAlignmentTool.vue
```

**页面结构**:
```vue
<template>
  <div class="pi-planning">
    <!-- 头部 -->
    <el-card>
      <template #header>
        <div style="display: flex; justify-content: space-between;">
          <h2>PI规划</h2>
          <el-button type="primary" @click="regeneratePIs">
            <el-icon><Refresh /></el-icon>
            重新生成PI
          </el-button>
        </div>
      </template>
      
      <el-alert type="info" :closable="false">
        <template #title>
          ℹ️ PI由同期产品版本自动组成，每个PI对齐一个里程碑
        </template>
      </el-alert>
    </el-card>
    
    <!-- PI列表 -->
    <el-row :gutter="16" style="margin-top: 16px;">
      <el-col 
        v-for="pi in pis" 
        :key="pi.id" 
        :span="8"
      >
        <PICard 
          :pi="pi" 
          @click="handlePIClick"
          @edit="handlePIEdit"
        />
      </el-col>
    </el-row>
    
    <!-- 里程碑对齐工具 -->
    <el-card style="margin-top: 16px;">
      <template #header>里程碑对齐</template>
      <PIAlignmentTool 
        :pis="pis" 
        :milestones="milestones"
        :iterations="iterations"
      />
    </el-card>
  </div>
</template>
```

**PICard组件**:
```vue
<template>
  <el-card 
    class="pi-card" 
    :class="{ 'is-active': pi.status === 'active' }"
    @click="$emit('click', pi)"
  >
    <template #header>
      <div style="display: flex; justify-content: space-between;">
        <span>{{ pi.name }}</span>
        <el-tag :type="getStatusType(pi.status)">
          {{ getStatusText(pi.status) }}
        </el-tag>
      </div>
    </template>
    
    <el-descriptions :column="1">
      <el-descriptions-item label="时间范围">
        {{ formatDateRange(pi.startDate, pi.endDate) }}
      </el-descriptions-item>
      <el-descriptions-item label="迭代范围">
        迭代{{ getIterationNumber(pi.startIterationId) }} ~ 
        迭代{{ getIterationNumber(pi.endIterationId) }}
      </el-descriptions-item>
      <el-descriptions-item label="Sprint数量">
        {{ pi.sprints?.length || 0 }} 个Sprint
      </el-descriptions-item>
      <el-descriptions-item label="对齐里程碑">
        <el-tag type="success">{{ pi.milestone?.name }}</el-tag>
      </el-descriptions-item>
      <el-descriptions-item label="包含版本">
        <el-space wrap>
          <el-tag 
            v-for="version in getVersionsInPI(pi)" 
            :key="version.id"
            size="small"
          >
            {{ version.productName }} {{ version.versionNumber }}
          </el-tag>
        </el-space>
      </el-descriptions-item>
    </el-descriptions>
    
    <template #footer>
      <div style="text-align: right;">
        <el-button size="small" @click.stop="$emit('edit', pi)">
          编辑
        </el-button>
        <el-button 
          size="small" 
          type="primary"
          @click.stop="gotoPIPlanning(pi)"
        >
          进入PI Planning
        </el-button>
      </div>
    </template>
  </el-card>
</template>
```

**PIAlignmentTool组件**（可视化展示PI与里程碑的对齐关系）:
```vue
<template>
  <div class="pi-alignment-tool">
    <!-- 迭代轴 -->
    <IterationAxis 
      :iterations="iterations" 
      :milestones="milestones"
    />
    
    <!-- PI区间显示 -->
    <div class="pi-ranges">
      <div 
        v-for="pi in pis" 
        :key="pi.id"
        class="pi-range"
        :style="getPIRangeStyle(pi)"
      >
        <span>{{ pi.name }}</span>
        <el-icon class="milestone-icon">
          <Location />
        </el-icon>
        <span>{{ pi.milestone.name }}</span>
      </div>
    </div>
    
    <!-- 版本区间显示 -->
    <div class="version-ranges">
      <div 
        v-for="version in versions" 
        :key="version.id"
        class="version-range"
        :style="getVersionRangeStyle(version)"
      >
        {{ version.productName }} {{ version.versionNumber }}
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
function getPIRangeStyle(pi: PI) {
  // 计算PI在迭代轴上的位置和宽度
  const startIndex = iterations.value.findIndex(i => i.id === pi.startIterationId)
  const endIndex = iterations.value.findIndex(i => i.id === pi.endIterationId)
  
  const cellWidth = 100 / iterations.value.length
  const left = startIndex * cellWidth
  const width = (endIndex - startIndex + 1) * cellWidth
  
  return {
    left: `${left}%`,
    width: `${width}%`
  }
}
</script>
```

**工作量**: 2天

---

## Week 4: 需求管理完善

### Day 21-22: Epic管理

### Day 23-24: Feature管理

### Day 25-26: SSTS管理

（详细实现略，优先级P1）

---

## Week 5: 功能完善与集成

### Day 27-28: 完成度计算和实时更新

**任务**:
1. 完善完成度计算逻辑
2. 实现级联更新（SSTS → Feature → Epic → Version）
3. 添加WebSocket实时推送（可选）
4. 性能优化（大数据量场景）

### Day 29-30: 里程碑对齐算法优化

**任务**:
1. 完善±7天对齐算法
2. 支持手动调整对齐
3. 对齐冲突检测和提示

---

## Week 6: 测试与优化

### Day 31-33: E2E测试

**Playwright测试用例**:
1. 项目创建完整流程
2. 版本规划拖拽操作
3. PI自动生成验证
4. 完成度计算验证
5. 里程碑对齐验证

### Day 34-35: 性能优化

**优化点**:
1. 虚拟滚动（大量迭代场景）
2. 防抖/节流（拖拽操作）
3. 懒加载（数据分页）

### Day 36: Bug修复和上线准备

---

## 🎯 关键里程碑

| 里程碑 | 完成日期 | 交付内容 | 验收标准 |
|--------|---------|---------|---------|
| **M1: 核心组件** | Week 2 | IterationAxis, VersionGantt, ProjectWizard | 组件功能完整，单元测试通过 |
| **M2: 核心页面** | Week 4 | VersionPlanning, ProjectDetail, PIPlanning | 页面功能完整，交互流畅 |
| **M3: 功能完善** | Week 5 | 完成度计算、里程碑对齐 | 算法正确，数据准确 |
| **M4: MVP发布** | Week 6 | 端到端功能 | E2E测试通过，用户验收通过 |

---

## 📊 工作量估算

### 人员配置建议

**方案A：1人全职**
- 前端开发工程师 × 1人
- 预计时间：6周（30个工作日）

**方案B：2人协作（推荐）⭐**
- 前端开发A（负责组件层）× 1人
- 前端开发B（负责页面层）× 1人
- 预计时间：4周（20个工作日）

**方案C：3人快速交付**
- 前端开发A（组件层）× 1人
- 前端开发B（页面层）× 1人
- 前端开发C（测试+优化）× 1人
- 预计时间：3周（15个工作日）

---

## 🔧 技术准备

### 开发环境
- Node.js 18+
- npm/yarn/pnpm
- VS Code + Volar插件
- Vue DevTools

### 依赖库
```json
{
  "dependencies": {
    "vue": "^3.3.0",
    "typescript": "^5.0.0",
    "pinia": "^2.1.0",
    "vue-router": "^4.0.0",
    "element-plus": "^2.3.0",
    "axios": "^1.4.0",
    "dayjs": "^1.11.0",
    "echarts": "^5.4.0",
    "vuedraggable": "^4.1.0"
  },
  "devDependencies": {
    "vite": "^4.3.0",
    "vitest": "^0.32.0",
    "@playwright/test": "^1.35.0"
  }
}
```

---

## 📚 参考资源

### 设计文档（必读）
1. `domain-proj-workspace/领域项目管理-V3新方案.md` - 完整架构设计
2. `domain-proj-workspace/项目建立流程.md` - 项目创建流程
3. `domain-proj-workspace/版本规划流程.md` - 版本规划流程
4. `domain-proj-workspace/前端数据模型设计.md` - TypeScript接口定义

### 代码示例
- `frontend/src/views/C3-Planning/PIPlanningStage1.vue` - 拖拽交互参考
- `frontend/src/stores/modules/pi.ts` - Store实现参考

---

## ✅ 成功标准

### 功能完整性
- ✅ 项目创建5步向导完整可用
- ✅ 版本规划工作台核心功能完整
- ✅ PI自动生成算法正确
- ✅ 完成度计算准确
- ✅ 里程碑对齐算法正确

### 用户体验
- ✅ 拖拽操作流畅
- ✅ 视觉反馈及时
- ✅ 错误提示友好
- ✅ 响应时间<2秒

### 代码质量
- ✅ TypeScript类型完整
- ✅ 单元测试覆盖率>80%
- ✅ E2E测试用例完整
- ✅ 代码规范通过ESLint

---

## 🚀 开始行动！

### 第一天就这么做：

1. **拉取最新代码**
```bash
git pull origin feature/domain-project-planning-1
```

2. **阅读核心设计文档（1小时）**
```bash
cat domain-proj-workspace/领域项目管理-V3新方案.md | head -500
```

3. **创建开发分支**
```bash
git checkout -b feature/version-planning-workspace
```

4. **开始编写IterationAxis组件**
```bash
mkdir -p frontend/src/components/project
touch frontend/src/components/project/IterationAxis.vue
```

5. **参考PI Planning代码**
```bash
cat frontend/src/views/C3-Planning/PIPlanningStage1.vue | grep -A 20 "draggable"
```

---

**💪 加油！6周后见证奇迹！**

---

**文档作者**: AI Assistant  
**文档日期**: 2026-01-19  
**文档版本**: v1.0

---

**END OF PLAN**
