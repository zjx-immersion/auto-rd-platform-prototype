# 前端实施方案评估报告

> **评估日期**: 2026-01-20  
> **评估范围**: 设计方案 → 现有frontend框架 → 实施方案  
> **评估目标**: 确定迭代、替换还是改造策略  
> **实施优先级**: P0核心功能优先

---

## 📊 现有Frontend框架分析

### 一、目录结构

```
frontend/src/
├── views/
│   ├── C0-Project/         ⭐ 领域项目管理（已有11个页面）
│   │   ├── ProjectCreate.vue        ✅ 4步向导式创建
│   │   ├── ProjectDetail.vue        ✅ Tab式详情页
│   │   ├── ProjectList.vue          ✅ 项目列表
│   │   ├── ProjectMonitor.vue       ✅ 项目监控
│   │   ├── VersionManagement.vue    ✅ 版本管理
│   │   ├── VersionPlanBoard.vue     ✅ 版本/PI规划工作台
│   │   ├── VersionList.vue          ✅ 版本列表
│   │   ├── PICreate.vue             ✅ PI创建
│   │   ├── PIList.vue               ✅ PI列表
│   │   ├── EpicAllocation.vue       ✅ Epic分配
│   │   └── FeatureAllocation.vue    ✅ Feature分配
│   ├── C1-Requirement/     ✅ 需求管理（13个页面）
│   ├── C2-Assets/          ✅ 资产管理（4个页面）
│   ├── C3-Planning/        ✅ 规划协调（13个页面）
│   ├── C4-Iteration/       ✅ 迭代执行（10个页面）
│   ├── C5-Testing/         ✅ 测试验证（6个页面）
│   ├── C6-DevOps/          ✅ 运维发布（4个页面）
│   └── C7-Analytics/       ✅ 数据分析（4个页面）
├── components/
│   ├── Layout/             ✅ 布局组件（Shell、导航等）
│   ├── Common/             ✅ 通用组件（PageContainer、PageHeader等）
│   └── Planning/           ✅ 规划组件（依赖图、冲突对话框）
├── stores/
│   └── modules/            ✅ 17个Store模块
├── router/
│   └── index.ts            ✅ 完整路由配置
├── types/
│   ├── entities.ts         ✅ 实体类型定义
│   └── domain-models.ts    ✅ 领域模型定义
└── mock-data/
    ├── datasets/           ✅ JSON数据集
    └── domain-*.json       ✅ 领域数据（4个JSON文件已存在）⭐
```

---

### 二、现有页面功能分析

#### 2.1 ProjectCreate.vue（现有）✅

**当前功能**:
```
步骤1: 基本信息
  - 项目名称、编码、领域、车型
  - 负责人、项目周期
  ✅ 已有实现

步骤2: 交付节点
  - 手工添加里程碑
  - 使用模板
  ✅ 已有实现（但无智能对齐）

步骤3: 团队配置
  - 团队选择
  ✅ 已有实现

步骤4: 完成创建
  - 信息确认
  ✅ 已有实现

缺失功能:
❌ PI时间线自动生成 ⭐（核心创新）
❌ 里程碑智能推荐 ⭐（核心创新）
❌ 固定12周节奏设置
```

**设计要求（C0-F01-V2）**:
```
步骤1: 基本信息（相同）
步骤2: PI框架设置 ⭐ 新增
  - 设置PI周期（默认12周）
  - 自动生成PI时间线
  - PITimelineGenerator组件
步骤3: 添加里程碑 ⭐ 增强
  - 添加里程碑
  - 里程碑智能推荐
  - MilestoneAlignmentChecker组件
步骤4: 团队配置（相同）
步骤5: 完成创建（相同）

实施策略: ⭐ 迭代增强（保留现有，新增步骤2，增强步骤3）
```

---

#### 2.2 ProjectDetail.vue（现有）✅

**当前功能**:
```
Tab 1: 基本信息
  - el-descriptions展示
  - 交付节点时间线（el-timeline）
  ✅ 已有实现

Tab 2: 版本管理
  - 版本列表（el-table）
  - 新建版本按钮
  ✅ 已有实现

Tab 3: PI规划
  - PI列表（el-table）
  - 新建PI按钮
  ✅ 已有实现

Tab 4: Epic管理
  - Epic列表
  ✅ 已有实现

缺失功能:
❌ Dashboard风格统计卡片
❌ PI时间线可视化（甘特图）⭐
❌ 快速操作入口
❌ 里程碑进度展示
❌ 团队负载展示
```

**设计要求（C0-F04-V2）**:
```
Tab 0: 概览（Dashboard）⭐ 新增
  - 快速统计卡片（4个）
  - 当前PI进度
  - 里程碑进度
  - 团队负载
  - 快速操作按钮

Tab 1: 版本列表（保留，增强）
Tab 2: PI列表（保留，增强）
  - PI时间线可视化（甘特图）⭐
Tab 3: 里程碑时间线（新增）
Tab 4: 团队（新增）

实施策略: ⭐ 迭代增强（新增Tab 0，增强Tab 1-2，新增Tab 3-4）
```

---

#### 2.3 VersionPlanBoard.vue（现有）✅

**当前功能**:
```
左侧:
  - 项目选择器
  - Epic/Feature列表（Tab切换）
  - 拖拽式分配
  ✅ 已有实现

右侧:
  - 时间线视图 / PI视图切换
  - 拖拽目标区域
  ✅ 已有实现

缺失功能:
❌ Epic完成度设置（0-100%）⭐⭐⭐⭐⭐（核心创新）
❌ Feature精细化设置 ⭐⭐⭐⭐⭐（核心创新）
❌ 完成度验证算法 ⭐
```

**设计要求（C0-F05-V2）**:
```
左侧: Epic选择器 + 完成度滑块 ⭐⭐⭐⭐⭐
  - EpicCompletionSetter组件
  - 0-100%任意完成度
  - Feature精细化按钮
  - FeatureCompletionDialog组件

右侧: 版本详情 + 完成度验证
  - 实时计算目标SP
  - 一致性验证
  - 累计分配验证

实施策略: ⭐⭐⭐⭐⭐ 新增页面（VersionPlanning-V2.vue）
           - 复用VersionPlanBoard.vue的拖拽逻辑
           - 新增完成度管理核心功能
```

---

#### 2.4 PICreate.vue（现有）✅

**当前功能**:
```
基本信息表单:
  - PI名称、编码、描述
  - 开始日期、结束日期
  ✅ 已有实现

缺失功能:
❌ 固定12周时间盒 ⭐
❌ 里程碑对齐检查 ⭐⭐⭐⭐⭐（核心创新）
❌ 3种调整方案建议 ⭐
❌ 版本范围选择
❌ PI目标设置
❌ 团队负载计算 ⭐
```

**设计要求（C0-F08-PI规划）**:
```
5步向导式创建:
步骤1: 基本信息 + 固定12周 ⭐
步骤2: 里程碑对齐检查 ⭐⭐⭐⭐⭐
  - MilestoneAlignmentChecker组件
  - 对齐算法（±7天/8-14天/>14天）
  - 3种调整方案建议
步骤3: 版本范围
  - 版本选择
  - Epic-PI分配
步骤4: PI目标与团队分配
  - PI目标设置
  - 团队负载计算 ⭐
步骤5: 确认创建

实施策略: ⭐⭐⭐⭐⭐ 新增页面（PIPlanning.vue）
           - 替换现有PICreate.vue
           - 完整实现5步向导
```

---

### 三、现有Store模块分析

#### 3.1 已有Store模块 ✅

```typescript
stores/modules/
├── project.ts      ✅ 项目管理
├── version.ts      ✅ 版本管理
├── pi.ts           ✅ PI管理
├── epic.ts         ✅ Epic管理
├── feature.ts      ✅ Feature管理
├── ssts.ts         ✅ SSTS管理
├── mr.ts           ✅ MR管理
├── team.ts         ✅ 团队管理
├── sprint.ts       ✅ Sprint管理
├── product.ts      ✅ 产品管理
├── user.ts         ✅ 用户管理
└── ... (其他7个Store)

需要增强的Store:
⚠️ version.ts - 需要增加完成度管理方法 ⭐⭐⭐⭐⭐
   - updateEpicCompletion()
   - validateCompletionConsistency()
   - calculateEpicCompletion()

⚠️ pi.ts - 需要增加里程碑对齐方法 ⭐
   - checkMilestoneAlignment()
   - generateSuggestions()
   - calculateTeamLoad()

⚠️ project.ts - 需要增加PI时间线生成方法 ⭐
   - generatePITimeline()
```

---

## 📋 实施方案设计

### 方案对比表

| 页面/组件 | 现有状态 | 设计要求 | 实施策略 | 优先级 | 工期 |
|----------|---------|---------|---------|--------|------|
| **ProjectCreate.vue** | ✅ 4步向导 | 5步向导 + PI自动生成⭐ | **迭代增强** | P0 | 2天 |
| **ProjectDetail.vue** | ✅ Tab式 | Dashboard + 时间线⭐ | **迭代增强** | P0 | 2天 |
| **VersionPlanning-V2.vue** | ❌ 无 | 完成度管理⭐⭐⭐⭐⭐ | **新增页面** | P0 | 3天 |
| **PIPlanning.vue** | ⚠️ 简单表单 | 5步向导 + 对齐⭐⭐⭐⭐⭐ | **替换页面** | P0 | 3天 |
| **VersionDetail.vue** | ❌ 无 | 完成度编辑 | **新增页面** | P1 | 1天 |
| **PIDetail.vue** | ❌ 无 | PI详情 + Planning入口 | **新增页面** | P1 | 1天 |
| **MilestoneManagement.vue** | ❌ 无 | 里程碑时间线 | **新增页面** | P2 | 1天 |

---

### 方案A: 迭代增强（推荐）⭐⭐⭐⭐⭐

#### 优点
```
✅ 保留现有功能，降低风险
✅ 用户体验平滑过渡
✅ 开发成本低（增量开发）
✅ 测试回归成本低
✅ 可分阶段上线
```

#### 实施步骤

**Phase 1: 基础设施（Day 1-2）**
```
1. 增强Store模块
   - version.ts: 增加完成度管理方法
   - pi.ts: 增加里程碑对齐方法
   - project.ts: 增加PI时间线生成方法

2. 创建核心组件
   - components/DomainProject/
     ├── PITimelineGenerator.vue ⭐
     ├── MilestoneAlignmentChecker.vue ⭐
     ├── EpicCompletionSetter.vue ⭐⭐⭐⭐⭐
     ├── FeatureCompletionDialog.vue ⭐⭐⭐⭐⭐
     ├── TeamLoadCalculator.vue ⭐
     ├── PITimelineVisualization.vue
     └── DashboardCards.vue
```

---

**Phase 2: 核心页面实现（Day 3-8）**

##### 2.1 迭代增强ProjectCreate.vue（Day 3-4）

```vue
<!-- ProjectCreate.vue 增强 -->
<template>
  <PageContainer>
    <el-steps :active="currentStep">
      <el-step title="基本信息" />
      <el-step title="PI框架设置" />  <!-- 新增步骤 ⭐ -->
      <el-step title="添加里程碑" />  <!-- 增强步骤 ⭐ -->
      <el-step title="团队配置" />
      <el-step title="完成创建" />
    </el-steps>
    
    <!-- 步骤1: 基本信息（保留现有） -->
    <div v-show="currentStep === 0">
      <!-- 现有代码 -->
    </div>
    
    <!-- 步骤2: PI框架设置（新增）⭐ -->
    <div v-show="currentStep === 1">
      <h3>PI框架设置</h3>
      <el-form>
        <el-form-item label="PI周期">
          <el-input-number v-model="piCycle" :min="8" :max="16" />
          <span>周（推荐12周）</span>
        </el-form-item>
        <el-form-item label="PI数量">
          <el-input-number v-model="piCount" :min="1" :max="8" />
        </el-form-item>
      </el-form>
      
      <!-- PI时间线自动生成组件 ⭐ -->
      <PITimelineGenerator
        :project-start="basicForm.dateRange[0]"
        :project-end="basicForm.dateRange[1]"
        :pi-cycle="piCycle"
        @update="handlePITimelineUpdate"
      />
    </div>
    
    <!-- 步骤3: 添加里程碑（增强）⭐ -->
    <div v-show="currentStep === 2">
      <h3>添加里程碑</h3>
      <!-- 现有代码 -->
      
      <!-- 里程碑智能推荐组件 ⭐ -->
      <MilestoneAlignmentChecker
        :pi-timeline="piTimeline"
        :milestones="milestones"
        @update="handleMilestoneAlignmentUpdate"
      />
    </div>
    
    <!-- 步骤4-5: 保留现有 -->
  </PageContainer>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import { useProjectStore } from '@/stores/modules/project'
import PITimelineGenerator from '@/components/DomainProject/PITimelineGenerator.vue'
import MilestoneAlignmentChecker from '@/components/DomainProject/MilestoneAlignmentChecker.vue'

const projectStore = useProjectStore()
const currentStep = ref(0)
const piCycle = ref(12)  // 默认12周
const piTimeline = ref([])

// PI时间线生成处理
function handlePITimelineUpdate(timeline: any[]) {
  piTimeline.value = timeline
  console.log('PI时间线已生成:', timeline)
}

// 里程碑对齐处理
function handleMilestoneAlignmentUpdate(alignments: any[]) {
  console.log('里程碑对齐结果:', alignments)
}
</script>
```

**修改内容**:
- 新增步骤2: PI框架设置
- 增强步骤3: 里程碑智能推荐
- 引入2个核心组件

**工期**: 2天

---

##### 2.2 迭代增强ProjectDetail.vue（Day 5-6）

```vue
<!-- ProjectDetail.vue 增强 -->
<template>
  <PageContainer v-loading="loading">
    <el-tabs v-model="activeTab">
      <!-- 新增Tab 0: 概览（Dashboard）⭐ -->
      <el-tab-pane label="概览" name="overview">
        <!-- 快速统计卡片 -->
        <DashboardCards :project="project" />
        
        <el-row :gutter="16">
          <el-col :span="12">
            <!-- 当前PI进度 -->
            <el-card>
              <h3>当前PI进度</h3>
              <CurrentPIProgress :pi="currentPI" />
            </el-card>
          </el-col>
          <el-col :span="12">
            <!-- 里程碑进度 -->
            <el-card>
              <h3>里程碑进度</h3>
              <MilestoneProgress :milestones="milestones" />
            </el-card>
          </el-col>
        </el-row>
        
        <!-- 快速操作 -->
        <el-card>
          <el-button type="primary" @click="handleCreateVersion">创建版本</el-button>
          <el-button type="primary" @click="handleCreatePI">创建PI</el-button>
          <el-button @click="handleAddMilestone">添加里程碑</el-button>
          <el-button @click="goToPIPlanning">进入PI Planning</el-button>
        </el-card>
      </el-tab-pane>
      
      <!-- Tab 1: 基本信息（保留现有） -->
      <el-tab-pane label="基本信息" name="basic">
        <!-- 现有代码 -->
      </el-tab-pane>
      
      <!-- Tab 2: 版本（增强）-->
      <el-tab-pane label="版本" name="versions">
        <!-- 现有代码 + 版本卡片增强显示 -->
      </el-tab-pane>
      
      <!-- Tab 3: PI（增强）⭐ -->
      <el-tab-pane label="PI" name="pis">
        <!-- PI时间线可视化 ⭐ -->
        <PITimelineVisualization
          :pis="pis"
          :milestones="milestones"
          :project-start="project.startDate"
          :project-end="project.endDate"
          @pi-click="handlePIClick"
        />
        
        <!-- PI列表（保留现有） -->
        <el-table :data="pis">
          <!-- 现有代码 -->
        </el-table>
      </el-tab-pane>
      
      <!-- Tab 4: 里程碑（新增）-->
      <el-tab-pane label="里程碑" name="milestones">
        <MilestoneTimeline :milestones="milestones" :pis="pis" />
      </el-tab-pane>
      
      <!-- Tab 5: 团队（新增）-->
      <el-tab-pane label="团队" name="teams">
        <TeamLoadOverview :teams="teams" :current-pi="currentPI" />
      </el-tab-pane>
    </el-tabs>
  </PageContainer>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { useProjectStore } from '@/stores/modules/project'
import DashboardCards from '@/components/DomainProject/DashboardCards.vue'
import PITimelineVisualization from '@/components/DomainProject/PITimelineVisualization.vue'
// ... 其他导入

const route = useRoute()
const router = useRouter()
const projectStore = useProjectStore()

const activeTab = ref('overview')  // 默认显示概览
const project = computed(() => projectStore.currentProject)
const currentPI = computed(() => projectStore.currentPI)
</script>
```

**修改内容**:
- 新增Tab 0: 概览（Dashboard）
- 增强Tab 3: PI时间线可视化
- 新增Tab 4-5: 里程碑、团队
- 引入3个核心组件

**工期**: 2天

---

##### 2.3 新增VersionPlanning-V2.vue（Day 7-9）⭐⭐⭐⭐⭐ 核心

```vue
<!-- views/C0-Project/VersionPlanning-V2.vue -->
<template>
  <PageContainer>
    <PageHeader title="版本规划" description="基于完成度管理的灵活版本规划">
      <template #actions>
        <el-button @click="handleSave" type="primary">保存规划</el-button>
      </template>
    </PageHeader>
    
    <el-row :gutter="16">
      <!-- 左侧: Epic选择与完成度设置 ⭐⭐⭐⭐⭐ -->
      <el-col :span="8">
        <el-card>
          <template #header>
            <span>Epic完成度管理</span>
          </template>
          
          <div class="epic-list">
            <div v-for="epic in epics" :key="epic.id" class="epic-item">
              <!-- Epic完成度设置组件 ⭐⭐⭐⭐⭐ -->
              <EpicCompletionSetter
                :epic="epic"
                :version-id="currentVersionId"
                @update="handleEpicCompletionUpdate"
                @feature-detail="handleFeatureDetailClick"
              />
            </div>
          </div>
        </el-card>
      </el-col>
      
      <!-- 右侧: 版本详情与验证 -->
      <el-col :span="16">
        <el-card>
          <template #header>
            <span>版本V1.0详情</span>
          </template>
          
          <!-- 版本信息卡片 -->
          <el-descriptions :column="2" border>
            <el-descriptions-item label="版本号">V1.0</el-descriptions-item>
            <el-descriptions-item label="目标日期">2025-06-30</el-descriptions-item>
            <el-descriptions-item label="关联里程碑">工程样车</el-descriptions-item>
            <el-descriptions-item label="总SP">{{ totalSP }} SP</el-descriptions-item>
          </el-descriptions>
          
          <!-- Epic分配列表 -->
          <h3>Epic分配</h3>
          <el-table :data="epicAllocations" border>
            <el-table-column prop="epicName" label="Epic名称" width="200" />
            <el-table-column label="完成度目标" width="150">
              <template #default="{ row }">
                <el-tag type="success" size="large">{{ row.completionTarget }}%</el-tag>
              </template>
            </el-table-column>
            <el-table-column prop="totalSP" label="总SP" width="100" />
            <el-table-column prop="targetSP" label="目标SP" width="100">
              <template #default="{ row }">
                <strong>{{ row.targetSP }} SP</strong>
              </template>
            </el-table-column>
            <el-table-column label="实际进度" width="200">
              <template #default="{ row }">
                <el-progress :percentage="row.actualProgress" />
              </template>
            </el-table-column>
          </el-table>
          
          <!-- 完成度验证 ⭐ -->
          <el-card style="margin-top: 16px">
            <h4>完成度验证</h4>
            <el-alert
              v-if="validationResult.isValid"
              type="success"
              :closable="false"
            >
              ✓ 完成度一致性验证通过
            </el-alert>
            <el-alert
              v-else
              type="warning"
              :closable="false"
            >
              ⚠️ {{ validationResult.message }}
            </el-alert>
          </el-card>
        </el-card>
      </el-col>
    </el-row>
    
    <!-- Feature精细化对话框 ⭐⭐⭐⭐⭐ -->
    <FeatureCompletionDialog
      v-model="featureDialogVisible"
      :epic="selectedEpic"
      :features="selectedFeatures"
      @update="handleFeatureCompletionUpdate"
    />
  </PageContainer>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { useVersionStore } from '@/stores/modules/version'
import { useEpicStore } from '@/stores/modules/epic'
import { useFeatureStore } from '@/stores/modules/feature'
import EpicCompletionSetter from '@/components/DomainProject/EpicCompletionSetter.vue'
import FeatureCompletionDialog from '@/components/DomainProject/FeatureCompletionDialog.vue'

const versionStore = useVersionStore()
const epicStore = useEpicStore()
const featureStore = useFeatureStore()

const currentVersionId = ref('VER-2025-001')
const featureDialogVisible = ref(false)
const selectedEpic = ref(null)

// Epic完成度更新
function handleEpicCompletionUpdate(epicId: string, completionTarget: number) {
  versionStore.updateEpicCompletion(currentVersionId.value, epicId, completionTarget)
}

// Feature精细化设置
function handleFeatureDetailClick(epic: any) {
  selectedEpic.value = epic
  selectedFeatures.value = featureStore.getFeaturesByEpicId(epic.id)
  featureDialogVisible.value = true
}

// Feature完成度更新
function handleFeatureCompletionUpdate(epicId: string, features: any[]) {
  versionStore.updateFeatureCompletions(currentVersionId.value, epicId, features)
}

// 完成度验证
const validationResult = computed(() => {
  return versionStore.validateCompletionConsistency(currentVersionId.value)
})
</script>
```

**核心功能**:
- ✅ Epic完成度滑块（0-100%）⭐⭐⭐⭐⭐
- ✅ Feature精细化设置 ⭐⭐⭐⭐⭐
- ✅ 完成度验证算法 ⭐
- ✅ 实时计算目标SP

**工期**: 3天

---

##### 2.4 新增PIPlanning.vue（Day 10-12）⭐⭐⭐⭐⭐ 核心

```vue
<!-- views/C0-Project/PIPlanning.vue -->
<template>
  <PageContainer>
    <PageHeader title="创建PI" description="基于固定节奏和里程碑映射的PI规划">
      <template #actions>
        <el-button @click="handleCancel">取消</el-button>
        <el-button @click="handleSaveDraft">保存草稿</el-button>
        <el-button @click="handleCreatePI" type="primary">创建PI</el-button>
      </template>
    </PageHeader>
    
    <!-- 5步向导 -->
    <el-card>
      <el-steps :active="currentStep" finish-status="success">
        <el-step title="基本信息" />
        <el-step title="里程碑对齐" />  <!-- ⭐⭐⭐⭐⭐ 核心 -->
        <el-step title="版本范围" />
        <el-step title="PI目标与团队" />
        <el-step title="确认创建" />
      </el-steps>
    </el-card>
    
    <el-card style="margin-top: 16px">
      <!-- 步骤1: 基本信息 + 固定12周 ⭐ -->
      <div v-show="currentStep === 0">
        <h3>基本信息</h3>
        <el-form label-width="120px">
          <el-form-item label="PI编号">
            <el-input v-model="piForm.piNumber" disabled>
              <template #append>自动生成</template>
            </el-input>
          </el-form-item>
          <el-form-item label="PI名称">
            <el-input v-model="piForm.piName" placeholder="例如: H56工程样车功能完成" />
          </el-form-item>
          <el-form-item label="起始日期">
            <el-date-picker v-model="piForm.startDate" type="date" disabled />
            <span style="margin-left: 8px">（上一个PI结束后第二天）</span>
          </el-form-item>
          <el-form-item label="结束日期">
            <el-date-picker v-model="piForm.endDate" type="date" disabled />
            <span style="margin-left: 8px">（固定12周）⭐</span>
          </el-form-item>
          <el-form-item label="PI周期">
            <el-tag type="info" size="large">12周（固定）🔒</el-tag>
          </el-form-item>
        </el-form>
      </div>
      
      <!-- 步骤2: 里程碑对齐检查 ⭐⭐⭐⭐⭐ 核心功能 -->
      <div v-show="currentStep === 1">
        <h3>里程碑对齐检查</h3>
        
        <!-- 里程碑对齐检查组件 ⭐⭐⭐⭐⭐ -->
        <MilestoneAlignmentChecker
          :pi-start-date="piForm.startDate"
          :pi-end-date="piForm.endDate"
          :pi-duration="12"
          :milestones="availableMilestones"
          @update="handleMilestoneAlignmentUpdate"
        />
      </div>
      
      <!-- 步骤3: 版本范围 -->
      <div v-show="currentStep === 2">
        <h3>版本范围</h3>
        <el-form label-width="120px">
          <el-form-item label="关联版本">
            <el-radio-group v-model="piForm.linkedVersionId">
              <el-radio label="">不关联版本</el-radio>
              <el-radio
                v-for="version in versions"
                :key="version.id"
                :label="version.id"
              >
                {{ version.name }}
              </el-radio>
            </el-radio-group>
          </el-form-item>
        </el-form>
        
        <!-- Epic分配 -->
        <el-table v-if="piForm.linkedVersionId" :data="versionEpics" border>
          <el-table-column type="selection" width="55" />
          <el-table-column prop="epicName" label="Epic名称" />
          <el-table-column label="完成度目标">
            <template #default="{ row }">
              <el-tag type="success">{{ row.completionTarget }}%</el-tag>
            </template>
          </el-table-column>
          <el-table-column prop="targetSP" label="目标SP" />
        </el-table>
      </div>
      
      <!-- 步骤4: PI目标与团队负载 ⭐ -->
      <div v-show="currentStep === 3">
        <h3>PI目标</h3>
        <el-button @click="handleAddObjective" type="primary">添加目标</el-button>
        <!-- PI目标列表 -->
        
        <h3 style="margin-top: 24px">团队负载</h3>
        <!-- 团队负载计算器 ⭐ -->
        <TeamLoadCalculator
          :teams="teams"
          :allocated-epics="selectedEpics"
          :pi-duration="12"
          @update="handleTeamLoadUpdate"
        />
      </div>
      
      <!-- 步骤5: 确认创建 -->
      <div v-show="currentStep === 4">
        <h3>确认信息</h3>
        <!-- 信息摘要 -->
      </div>
      
      <!-- 步骤操作按钮 -->
      <div class="step-actions">
        <el-button v-if="currentStep > 0" @click="handlePrevStep">上一步</el-button>
        <el-button v-if="currentStep < 4" @click="handleNextStep" type="primary">下一步</el-button>
        <el-button v-if="currentStep === 4" @click="handleCreatePI" type="primary">创建PI</el-button>
      </div>
    </el-card>
  </PageContainer>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { usePIStore } from '@/stores/modules/pi'
import MilestoneAlignmentChecker from '@/components/DomainProject/MilestoneAlignmentChecker.vue'
import TeamLoadCalculator from '@/components/DomainProject/TeamLoadCalculator.vue'

const router = useRouter()
const piStore = usePIStore()
const currentStep = ref(0)

// PI表单
const piForm = ref({
  piNumber: 'PI-2',
  piName: '',
  startDate: '2025-04-28',
  endDate: '2025-07-20',  // 固定12周
  linkedVersionId: '',
  linkedMilestoneId: '',
  objectives: [],
  teamAllocations: []
})

// 里程碑对齐更新
function handleMilestoneAlignmentUpdate(selectedMilestones: any[], decisions: any[]) {
  console.log('里程碑对齐决策:', decisions)
  piForm.value.linkedMilestoneId = selectedMilestones[0]?.milestoneId || ''
}

// 团队负载更新
function handleTeamLoadUpdate(teamAllocations: any[]) {
  piForm.value.teamAllocations = teamAllocations
}

// 创建PI
async function handleCreatePI() {
  await piStore.createPI(piForm.value)
  router.push({ name: 'ProjectDetail', params: { id: projectId } })
}
</script>
```

**核心功能**:
- ✅ 固定12周时间盒 ⭐
- ✅ 里程碑对齐检查 ⭐⭐⭐⭐⭐
- ✅ 3种调整方案建议 ⭐
- ✅ 团队负载计算 ⭐

**工期**: 3天

---

**Phase 3: 详情页与辅助功能（Day 13-15）**

##### 3.1 新增VersionDetail.vue（Day 13）

```vue
<!-- views/C0-Project/VersionDetail.vue -->
<template>
  <PageContainer>
    <PageHeader :title="`版本${version.name}详情`">
      <template #actions>
        <el-button @click="handleEdit">编辑完成度</el-button>
        <el-button @click="handleSave" type="primary">保存</el-button>
      </template>
    </PageHeader>
    
    <!-- 版本信息 -->
    <el-card>
      <el-descriptions :column="4" border>
        <el-descriptions-item label="版本号">{{ version.name }}</el-descriptions-item>
        <el-descriptions-item label="目标日期">{{ version.targetDate }}</el-descriptions-item>
        <el-descriptions-item label="关联里程碑">{{ milestoneName }}</el-descriptions-item>
        <el-descriptions-item label="总SP">{{ version.totalSP }} SP</el-descriptions-item>
      </el-descriptions>
    </el-card>
    
    <!-- Epic完成度管理 -->
    <el-card style="margin-top: 16px">
      <template #header>
        <span>Epic完成度管理</span>
        <el-button style="float: right" @click="handleEditCompletion">编辑完成度</el-button>
      </template>
      
      <div v-for="epic in epicAllocations" :key="epic.epicId">
        <EpicCompletionSetter
          :epic="epic"
          :version-id="versionId"
          :readonly="!editMode"
          @update="handleEpicCompletionUpdate"
        />
      </div>
    </el-card>
    
    <!-- 版本进度统计 -->
    <el-card style="margin-top: 16px">
      <h3>版本进度统计</h3>
      <el-progress :percentage="version.overallProgress" />
      <!-- 更多统计 -->
    </el-card>
  </PageContainer>
</template>
```

**工期**: 1天

---

##### 3.2 新增PIDetail.vue（Day 14）

```vue
<!-- views/C0-Project/PIDetail.vue -->
<template>
  <PageContainer>
    <PageHeader :title="`PI-${pi.piNumber}详情`">
      <template #actions>
        <el-button @click="handleEdit">编辑</el-button>
        <el-button @click="goToPIPlanning" type="primary">进入PI Planning</el-button>
      </template>
    </PageHeader>
    
    <!-- PI信息卡片 -->
    <el-row :gutter="16">
      <el-col :span="6">
        <el-card>
          <el-statistic title="PI编号" :value="pi.piNumber" />
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <el-statistic title="时间范围" :value="`${pi.startDate} ~ ${pi.endDate}`" />
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <el-statistic title="关联版本" :value="versionName" />
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <el-statistic title="总SP" :value="pi.totalSP" suffix="SP" />
        </el-card>
      </el-col>
    </el-row>
    
    <!-- PI目标 -->
    <el-card style="margin-top: 16px">
      <h3>PI目标</h3>
      <el-table :data="pi.objectives" border>
        <el-table-column prop="description" label="目标描述" />
        <el-table-column prop="businessValue" label="业务价值" width="120" />
        <el-table-column prop="confidence" label="信心指数" width="120" />
        <el-table-column prop="status" label="状态" width="120" />
      </el-table>
    </el-card>
    
    <!-- 团队负载 -->
    <el-card style="margin-top: 16px">
      <h3>团队分配与负载</h3>
      <el-table :data="pi.teamAllocations" border>
        <el-table-column prop="teamName" label="团队名称" />
        <el-table-column prop="allocatedSP" label="分配SP" />
        <el-table-column label="负载率" width="300">
          <template #default="{ row }">
            <el-progress :percentage="row.loadRate" :status="getLoadStatus(row.loadRate)" />
          </template>
        </el-table-column>
      </el-table>
    </el-card>
  </PageContainer>
</template>
```

**工期**: 1天

---

### 四、路由配置更新

```typescript
// router/index.ts 新增路由
{
  path: 'function',
  children: [
    {
      path: 'c0-project',
      children: [
        // ... 现有路由

        // 新增路由
        {
          path: 'version/planning-v2',
          name: 'VersionPlanningV2',
          component: () => import('@/views/C0-Project/VersionPlanning-V2.vue'),
          meta: { 
            title: '版本规划V2（完成度管理）', 
            breadcrumb: ['固有功能', 'C0: 领域项目管理', '版本规划V2'] 
          }
        },
        {
          path: 'pi/planning',
          name: 'PIPlanning',
          component: () => import('@/views/C0-Project/PIPlanning.vue'),
          meta: { 
            title: 'PI规划（里程碑映射）', 
            breadcrumb: ['固有功能', 'C0: 领域项目管理', 'PI规划'] 
          }
        },
        {
          path: 'version/detail/:id',
          name: 'VersionDetail',
          component: () => import('@/views/C0-Project/VersionDetail.vue'),
          meta: { 
            title: '版本详情', 
            breadcrumb: ['固有功能', 'C0: 领域项目管理', '版本详情'] 
          }
        },
        {
          path: 'pi/detail/:id',
          name: 'PIDetail',
          component: () => import('@/views/C0-Project/PIDetail.vue'),
          meta: { 
            title: 'PI详情', 
            breadcrumb: ['固有功能', 'C0: 领域项目管理', 'PI详情'] 
          }
        }
      ]
    }
  ]
}
```

---

## 📅 实施时间表

### 总体时间线（15天）

```
Week 1 (Day 1-5): 基础设施 + 项目创建/详情
├── Day 1-2: Store增强 + 核心组件框架
├── Day 3-4: ProjectCreate.vue迭代增强
└── Day 5: ProjectDetail.vue迭代增强

Week 2 (Day 6-10): 核心页面实现
├── Day 6: ProjectDetail.vue完成
├── Day 7-9: VersionPlanning-V2.vue ⭐⭐⭐⭐⭐
└── Day 10: PIPlanning.vue开始

Week 3 (Day 11-15): PI规划 + 详情页
├── Day 11-12: PIPlanning.vue完成 ⭐⭐⭐⭐⭐
├── Day 13: VersionDetail.vue
├── Day 14: PIDetail.vue
└── Day 15: 集成测试 + Bug修复
```

---

## 📊 风险评估与应对

### 高风险项

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 完成度管理算法复杂 | 中 | 高 | 预先实现算法，单元测试充分 |
| 里程碑对齐算法调试 | 中 | 高 | 参考设计文档，逐步验证 |
| 现有页面兼容性 | 低 | 中 | 保留现有功能，增量开发 |
| 用户习惯改变 | 中 | 低 | 提供教程，分阶段上线 |

---

## ✅ 最终建议

### 推荐方案：迭代增强 + 新增页面 ⭐⭐⭐⭐⭐

```
实施策略:
✅ 迭代增强: ProjectCreate.vue、ProjectDetail.vue
✅ 新增页面: VersionPlanning-V2.vue、PIPlanning.vue
✅ 新增详情: VersionDetail.vue、PIDetail.vue
✅ 新增组件: 7个核心组件

优点:
✅ 风险可控（保留现有功能）
✅ 用户体验平滑
✅ 开发效率高
✅ 测试成本低
✅ 可分阶段上线

总工期: 15天
核心功能: 12天（Day 1-12）
辅助功能: 3天（Day 13-15）
```

---

**评估日期**: 2026-01-20  
**评估结论**: ✅ **方案可行，推荐立即实施**  
**预计工期**: **15天**  
**下一步**: 开始Phase 1基础设施开发
