# 端到端测试用例设计文档

> **文档版本**: 1.0.0  
> **创建日期**: 2026-01-17  
> **测试目标**: 验证从领域项目建立到PI Planning分工到团队迭代计划的完整流程  
> **参考数据模型**: `frontend/src/types/domain-models.ts`

---

## 📋 测试目标

设计一套完整的端到端测试用例，验证以下完整业务流程：

```
领域项目建立 → Epic创建和关联 → Feature拆解 → SSTS拆解 → MR创建 
→ PI版本创建 → PI Planning → Feature分配到PI和Sprint 
→ MR分配到团队 → Task创建和分配 → 团队迭代计划
```

---

## 🎯 测试覆盖范围

### Phase 1: 领域项目建立

**测试用例**: `1.1 导航到项目列表并验证数据`、`1.2 查看项目详情并验证Epic关联`

**验证内容**:
- ✅ 项目列表页面加载
- ✅ 项目数据正确显示（编码、名称、车型、领域、状态、健康度等）
- ✅ 项目详情页面导航正常
- ✅ 项目与Epic的关联关系正确显示

**数据模型验证**:
```typescript
DomainProject {
  id, code, name, vehicleModel, domain,
  startDate, sopDate, endDate,
  status, health,
  epicIds[], // 关联的Epic列表
  piVersionIds[], // 关联的PI版本列表
  teams[], milestones[]
}
```

---

### Phase 2: Epic创建和关联

**测试用例**: `2.1 导航到Epic列表并验证数据`、`2.2 查看Epic详情并验证Feature关联`

**验证内容**:
- ✅ Epic列表页面加载
- ✅ Epic数据正确显示（编码、标题、项目、状态、优先级、故事点、进度、Features数量等）
- ✅ Epic详情页面导航正常（路由修复验证）
- ✅ Epic与Feature的关联关系正确显示（Feature Tab）
- ✅ Epic与项目的关联关系正确显示

**数据模型验证**:
```typescript
Epic {
  id, code, title, projectId,
  description, businessValue, acceptanceCriteria[],
  featureIds[], // 拆解出的Feature列表
  priority, status, storyPoints, completedStoryPoints, progress,
  targetPI, targetVersion,
  owner, stakeholders[], tags[]
}
```

**关键验证点**:
- Epic的`featureIds`数组与Feature的`epicId`字段形成双向关联
- Epic详情页面的Feature Tab显示关联的Feature列表
- Feature表格显示完整字段：编码、标题、产品线、产品、状态、优先级、故事点、复杂度、SSTS数量、负责人

---

### Phase 3: Feature拆解到SSTS

**测试用例**: `3.1 查看Feature详情并验证SSTS关联`

**验证内容**:
- ✅ Feature详情页面导航正常
- ✅ Feature基本信息正确显示（编码、所属Epic、产品线、产品、状态、优先级、故事点、复杂度、SSTS数量等）
- ✅ Feature与Epic的关联关系正确显示（Epic链接可点击）
- ✅ Feature与SSTS的关联关系正确显示（SSTS Tab）
- ✅ SSTS列表显示完整字段：编码、标题、类型、优先级、复杂度、状态、MR数量

**数据模型验证**:
```typescript
Feature {
  id, code, title, epicId,
  productLine, product, featureAssetId,
  prd: { content, version, status },
  acceptanceCriteria[],
  sstsIds[], // 拆解出的SSTS列表
  targetPI, targetVersion,
  status, owner, storyPoints, complexity,
  tags[]
}

SSTS {
  id, code, title, featureId,
  type: 'functional' | 'technical',
  specification: { content, scenarios?, performance?, safety?, interface? },
  mrIds[], // 分配到的MR列表
  priority, complexity, estimate,
  status, owner, dependencies[],
  targetPI, targetSprint, tags[]
}
```

**关键验证点**:
- Feature的`sstsIds`数组与SSTS的`featureId`字段形成双向关联
- Feature详情页面的SSTS Tab显示关联的SSTS列表
- SSTS表格显示完整字段：编码、标题、类型、优先级、复杂度、状态、MR数量

---

### Phase 4: PI版本创建

**测试用例**: `4.1 导航到PI列表并验证数据`、`4.2 验证PI与Epic和Feature的关联`

**验证内容**:
- ✅ PI列表页面加载
- ✅ PI数据正确显示（编码、名称、开始日期、结束日期、Sprint数量、关联项目等）
- ✅ PI与Epic的关联关系（Epic的`targetPI`字段）
- ✅ PI与Feature的关联关系（Feature的`targetPI`字段）

**数据模型验证**:
```typescript
PIVersion {
  id, code, name,
  startDate, endDate,
  sprintCount, sprintDuration,
  projectIds[], // 关联的项目列表
  objectives: [{ id, description, businessValue, status }],
  epicIds[], // 关联的Epic列表
  featureIds[], // 关联的Feature列表
  teams[]
}
```

**关键验证点**:
- Epic的`targetPI`字段与PI的`id`匹配
- Feature的`targetPI`字段与PI的`id`匹配
- PI的`epicIds`和`featureIds`数组包含关联的Epic和Feature

---

### Phase 5: PI Planning - Feature分配到PI和Sprint

**测试用例**: `5.1 导航到PI Planning看板`、`5.2 验证Feature分配到PI`

**验证内容**:
- ✅ PI Planning看板页面加载
- ✅ PI选择器功能正常
- ✅ Feature列表或看板显示
- ✅ 团队泳道或Sprint列显示
- ✅ Feature分配到PI的功能验证
- ✅ Feature分配到Sprint的功能验证

**数据模型验证**:
```typescript
// PI Planning结果
PIPlanningResult {
  piId, teams[], sprints[],
  dependencies[], risks[],
  status, createdAt
}

// 团队规划
TeamPlanning {
  teamId, teamName,
  features[], totalLoad, capacity
}

// Sprint规划
SprintPlanning {
  sprintId, sprintName,
  startDate, endDate,
  features[]
}
```

**关键验证点**:
- Feature通过`targetPI`字段关联到PI
- Feature通过`targetSprint`字段关联到Sprint
- PI Planning看板显示Feature在不同团队/Sprint的分配情况

---

### Phase 6: MR分配到团队

**测试用例**: `6.1 导航到MR列表并验证数据`、`6.2 验证MR与SSTS和团队的关联`

**验证内容**:
- ✅ MR列表页面加载
- ✅ MR数据正确显示（编码、标题、所属SSTS、团队、状态等）
- ✅ MR与SSTS的关联关系（SSTS的`mrIds`数组）
- ✅ MR与团队的关联关系（MR的`teamId`和`teamName`字段）

**数据模型验证**:
```typescript
MR {
  id, code, title, sstsId,
  moduleName, moduleAssetId,
  teamId, teamName, // 团队分配
  interfaceSpec, designDoc,
  dependencies[],
  effortHours,
  status, owner, assignee,
  taskIds[], // 拆解出的Task列表
  targetPI, targetSprint,
  tags[]
}
```

**关键验证点**:
- SSTS的`mrIds`数组与MR的`sstsId`字段形成双向关联
- MR的`teamId`和`teamName`字段正确显示团队信息
- MR通过`targetSprint`字段关联到Sprint

---

### Phase 7: Task创建和分配到Sprint

**测试用例**: `7.1 导航到Sprint列表并验证数据`、`7.2 验证Task列表和分配到Sprint`

**验证内容**:
- ✅ Sprint列表页面加载
- ✅ Sprint数据正确显示（编码、名称、所属PI、开始日期、结束日期、状态等）
- ✅ Task列表页面加载
- ✅ Task数据正确显示（编码、标题、所属MR、所属Sprint、分配人、状态、优先级等）
- ✅ Task与Sprint的关联关系（Task的`sprintId`字段）
- ✅ Task与MR的关联关系（Task的`mrId`字段）

**数据模型验证**:
```typescript
Sprint {
  id, code, name, piId,
  startDate, endDate, duration,
  capacity, plannedStoryPoints,
  completedStoryPoints, status,
  teamId, teamName
}

Task {
  id, code, title,
  mrId, sprintId, // 归属
  description, acceptanceCriteria,
  assignee, reviewer, // 分配
  estimateHours, actualHours, remainingHours,
  status: TaskStatus,
  priority,
  startDate, endDate, blockedDate,
  blocked, blockers[],
  parentTaskId, subTaskIds[], dependencies[],
  codeRepoUrl, branchName, pullRequestUrl, commits[],
  tags[], type
}
```

**关键验证点**:
- Task的`sprintId`字段与Sprint的`id`匹配
- Task的`mrId`字段与MR的`id`匹配
- Task的`assignee`字段显示分配人信息
- Task的状态流转正确

---

### Phase 8: 端到端数据流验证

**测试用例**: `8.1 完整数据流追溯`、`8.2 PI Planning完整流程验证`

**验证内容**:
- ✅ Project → Epic → Feature → SSTS → MR → Task 完整数据流
- ✅ 每个层级的关联关系正确
- ✅ 数据双向关联一致性
- ✅ PI Planning完整流程：PI创建 → Feature分配 → Sprint创建 → Task分配

**数据流验证路径**:
```
1. Project (proj-001)
   └── Epic (epic-001) [projectId: proj-001]
       └── Feature (feat-001) [epicId: epic-001]
           └── SSTS (ssts-001) [featureId: feat-001]
               └── MR (mr-001) [sstsId: ssts-001]
                   └── Task (task-001) [mrId: mr-001, sprintId: sprint-001]

2. PI (pi-001)
   ├── Epic (epic-001) [targetPI: pi-001]
   ├── Feature (feat-001) [targetPI: pi-001]
   └── Sprint (sprint-001) [piId: pi-001]
       └── Task (task-001) [sprintId: sprint-001]

3. Team (team-001)
   ├── MR (mr-001) [teamId: team-001]
   └── Task (task-001) [assignee: user-001]
```

---

### Phase 9: 数据关联完整性验证

**测试用例**: `9.1 验证所有实体字段的完整性和一致性`、`9.2 验证数据关联的双向一致性`

**验证内容**:
- ✅ 所有实体（Project、Epic、Feature、SSTS、MR、PI、Sprint、Task）的字段完整性
- ✅ 表格列与数据模型字段的一致性
- ✅ 双向关联的一致性（Epic ↔ Feature、Feature ↔ SSTS等）
- ✅ 关联字段的正确显示和导航

**字段完整性检查清单**:

| 实体 | 必填字段 | 关联字段 | 状态字段 | 时间字段 |
|------|---------|---------|---------|---------|
| **Project** | id, code, name, domain | epicIds[], piVersionIds[] | status, health | startDate, sopDate |
| **Epic** | id, code, title, projectId | featureIds[], targetPI | status | createdAt, updatedAt |
| **Feature** | id, code, title, epicId | sstsIds[], targetPI, targetVersion | status | createdAt, updatedAt |
| **SSTS** | id, code, title, featureId | mrIds[], targetPI, targetSprint | status | createdAt, updatedAt |
| **MR** | id, code, title, sstsId | taskIds[], teamId, targetSprint | status | createdAt, updatedAt |
| **PI** | id, code, name | projectIds[], epicIds[], featureIds[] | status | startDate, endDate |
| **Sprint** | id, code, name, piId | teamId | status | startDate, endDate |
| **Task** | id, code, title, mrId, sprintId | assignee, dependencies[] | status | startDate, endDate |

---

### Phase 10: 团队迭代计划验证

**测试用例**: `10.1 验证Sprint看板和Task分配`、`10.2 验证Task分配到团队成员`

**验证内容**:
- ✅ Sprint看板页面加载
- ✅ 看板列显示（待办、进行中、完成等）
- ✅ Task卡片在正确列中显示
- ✅ Task分配到团队成员的功能
- ✅ Task状态流转功能

**关键验证点**:
- Sprint看板显示Task的状态分布
- Task的`assignee`字段正确显示分配人
- Task的`sprintId`字段正确关联到Sprint
- Task的状态可以正确流转

---

## 🔍 测试用例详细设计

### 测试用例结构

每个测试用例包含以下部分：

1. **前置条件**: 页面导航和数据加载
2. **执行步骤**: 页面操作和数据验证
3. **验证点**: 数据字段、关联关系、功能正确性
4. **截图保存**: 关键步骤的截图保存

### 测试数据验证策略

#### 1. 数据存在性验证
- 验证列表页面显示数据
- 验证详情页面数据加载
- 验证Tab切换后数据加载

#### 2. 数据关联验证
- 验证父子关联（Epic → Feature → SSTS → MR → Task）
- 验证交叉关联（Epic → PI、Feature → PI、Feature → Sprint）
- 验证双向关联一致性

#### 3. 数据字段验证
- 验证必填字段存在
- 验证字段值正确性
- 验证字段显示格式

#### 4. 功能操作验证
- 验证页面导航
- 验证Tab切换
- 验证按钮点击
- 验证数据加载

---

## 📊 测试覆盖矩阵

| 测试阶段 | 实体 | 关联验证 | 字段验证 | 功能验证 |
|---------|------|---------|---------|---------|
| **Phase 1** | Project | Project → Epic | 项目字段 | 项目列表、详情 |
| **Phase 2** | Epic | Epic → Feature, Epic → Project | Epic字段 | Epic列表、详情、Feature Tab |
| **Phase 3** | Feature, SSTS | Feature → SSTS, Feature → Epic | Feature、SSTS字段 | Feature详情、SSTS Tab |
| **Phase 4** | PI | PI → Epic, PI → Feature | PI字段 | PI列表、关联验证 |
| **Phase 5** | PI Planning | Feature → PI, Feature → Sprint | Planning字段 | PI Planning看板 |
| **Phase 6** | MR | MR → SSTS, MR → Team | MR字段 | MR列表、团队分配 |
| **Phase 7** | Sprint, Task | Task → Sprint, Task → MR | Sprint、Task字段 | Sprint列表、Task列表 |
| **Phase 8** | 全流程 | 完整数据流 | 所有字段 | 端到端流程 |
| **Phase 9** | 所有实体 | 双向关联 | 字段完整性 | 数据一致性 |
| **Phase 10** | Sprint, Task | Task → Team | Task分配字段 | Sprint看板、Task分配 |

---

## 🎯 关键验证点

### 1. 路由修复验证

**验证内容**:
- ✅ Epic列表 → Epic详情路由正常（`/function/c1-requirement/epic/:id`）
- ✅ Epic详情 → Feature详情路由正常（`/function/c1-requirement/feature/:id`）
- ✅ Feature详情 → Epic详情路由正常（通过Epic链接）
- ✅ 无404错误

### 2. Tab功能验证

**验证内容**:
- ✅ Epic详情页面的Feature Tab正常显示和切换
- ✅ Feature详情页面的SSTS Tab正常显示和切换
- ✅ Tab中显示的数据关联正确
- ✅ Tab badge显示关联数量正确

### 3. 数据关联验证

**验证内容**:
- ✅ Epic的`featureIds`数组与Feature的`epicId`字段一致
- ✅ Feature的`sstsIds`数组与SSTS的`featureId`字段一致
- ✅ SSTS的`mrIds`数组与MR的`sstsId`字段一致
- ✅ MR的`taskIds`数组与Task的`mrId`字段一致

### 4. 字段映射验证

**验证内容**:
- ✅ Feature的`name`字段正确映射为`title`字段
- ✅ SSTS的`name`字段正确映射为`title`字段
- ✅ 所有字段在页面正确显示

---

## 📝 测试执行说明

### 运行测试

```bash
cd frontend
npx playwright test browser-test/tests/e2e-domain-to-pi-planning.spec.ts --project=chromium
```

### 测试输出

- **截图**: `browser-test/results/e2e-*.png`
- **测试报告**: Playwright HTML报告
- **控制台日志**: 详细的执行步骤和验证结果

### 测试环境要求

- 应用运行在 `http://localhost:6060`
- Mock数据已加载
- 数据关联已建立

---

## 🔄 数据流图

```
┌─────────────────────────────────────────────────────────────────┐
│                   端到端数据流验证流程                           │
└─────────────────────────────────────────────────────────────────┘

1. 领域项目建立
   Project (proj-001)
   ├── 字段: code, name, domain, status, health
   └── 关联: epicIds[]

2. Epic创建和关联
   Epic (epic-001)
   ├── 字段: code, title, projectId, status, priority, storyPoints
   ├── 关联: projectId → Project
   └── 关联: featureIds[] → Feature

3. Feature拆解
   Feature (feat-001)
   ├── 字段: code, title, epicId, productLine, product, status
   ├── 关联: epicId → Epic
   ├── 关联: sstsIds[] → SSTS
   └── 关联: targetPI → PI

4. SSTS拆解
   SSTS (ssts-001)
   ├── 字段: code, title, featureId, type, priority, complexity
   ├── 关联: featureId → Feature
   ├── 关联: mrIds[] → MR
   └── 关联: targetPI, targetSprint

5. MR创建和分配
   MR (mr-001)
   ├── 字段: code, title, sstsId, moduleName, teamId, status
   ├── 关联: sstsId → SSTS
   ├── 关联: teamId → Team
   ├── 关联: taskIds[] → Task
   └── 关联: targetSprint → Sprint

6. PI版本创建
   PI (pi-001)
   ├── 字段: code, name, startDate, endDate, sprintCount
   ├── 关联: projectIds[] → Project
   ├── 关联: epicIds[] → Epic
   └── 关联: featureIds[] → Feature

7. PI Planning
   ├── Feature分配到PI (Feature.targetPI = PI.id)
   ├── Feature分配到Sprint (Feature.targetSprint = Sprint.id)
   └── MR分配到团队 (MR.teamId = Team.id)

8. Sprint创建
   Sprint (sprint-001)
   ├── 字段: code, name, piId, startDate, endDate, status
   └── 关联: piId → PI

9. Task创建和分配
   Task (task-001)
   ├── 字段: code, title, mrId, sprintId, assignee, status
   ├── 关联: mrId → MR
   ├── 关联: sprintId → Sprint
   └── 关联: assignee → User

10. 团队迭代计划
    ├── Sprint看板显示Task状态
    ├── Task分配到团队成员
    └── Task状态流转
```

---

## ✅ 测试通过标准

### 功能验证标准

1. **页面导航**: 所有页面可以正常导航，无404错误
2. **数据加载**: 所有列表和详情页面数据正确加载
3. **Tab切换**: 所有Tab可以正常切换，数据正确显示
4. **数据关联**: 所有关联关系正确显示和验证
5. **字段显示**: 所有字段在页面正确显示

### 数据验证标准

1. **数据存在性**: 所有实体数据存在且可访问
2. **关联一致性**: 双向关联关系一致
3. **字段完整性**: 所有必填字段存在
4. **字段正确性**: 字段值符合数据模型定义

---

## 📚 参考文档

- [数据实体字段完整说明](./数据实体字段完整说明.md)
- [Epic和Feature详情页面完善完成报告](./Epic和Feature详情页面完善完成报告.md)
- [domain-models.ts](../../frontend/src/types/domain-models.ts)

---

**文档结束**
