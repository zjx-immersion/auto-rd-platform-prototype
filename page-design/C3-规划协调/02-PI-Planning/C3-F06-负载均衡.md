# C3-F06: 负载均衡

> **功能编号**: C3-F06  
> **功能名称**: 负载均衡  
> **所属模块**: C3-规划协调 > PI Planning  
> **优先级**: P0（核心功能）

---

## 功能概述

### 功能描述

负载均衡功能用于在PI Planning过程中，根据各团队的容量和Feature的工作量，智能地平衡团队间的工作负载。系统通过可视化的方式展示各团队的负载情况，支持拖拽调整Feature分配，确保各团队的负载处于合理范围内，避免某些团队过载而其他团队闲置。

### 业务价值

- **负载优化**: 确保各团队负载均衡，提高整体交付效率
- **风险识别**: 及时发现负载不均的问题，降低交付风险
- **资源利用**: 最大化团队资源利用率，避免浪费
- **决策支持**: 为Feature分配调整提供数据支持

### 使用场景

1. **PI Planning阶段**: 实时查看和调整团队负载
2. **Feature分配调整**: 根据负载情况重新分配Feature
3. **容量变更**: 团队容量变化后重新平衡负载
4. **风险评估**: 评估负载不均对PI目标的影响

---

## 页面布局

```
┌─────────────────────────────────────────────────────────────────┐
│  PI 2024.2 - 负载均衡                                             │
├─────────────────────────────────────────────────────────────────┤
│  [← 返回]  [智能平衡]  [重置]  [导出报告]                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  负载总览                                                          │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 负载均衡度: 78分 ⭐⭐⭐⭐     标准差: 12%                      │ │
│  │                                                             │ │
│  │ 团队负载分布:                                                │ │
│  │   🟢 健康(≤85%): 7个团队                                    │ │
│  │   🟡 警告(85-95%): 3个团队                                  │ │
│  │   🔴 超载(>95%): 2个团队                                    │ │
│  │                                                             │ │
│  │ 优化建议:                                                    │ │
│  │   • Team C超载(107%)，建议迁移2-3个Feature到Team E         │ │
│  │   • Team B接近满载(97%)，建议预留缓冲空间                   │ │
│  │   • Team F利用率较低(55%)，可承接更多Feature                │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  负载可视化                                                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 图表类型: [柱状图] [雷达图] [热力图]                          │ │
│  │                                                             │ │
│  │ 团队负载柱状图:                                              │ │
│  │                                                             │ │
│  │ 利                                                          │ │
│  │ 用 100%┼─────────────────────────────────── 超载线          │ │
│  │ 率  95%┼─────────────────────────────────── 警告线          │ │
│  │     85%┼─────────────────────────────────── 健康线          │ │
│  │        │                                                    │ │
│  │     🔴 │█████████████████████████ 107%  Team C             │ │
│  │     🟡 │███████████████████████   97%   Team B             │ │
│  │     🟡 │██████████████████████    95%   Team H             │ │
│  │     🟡 │█████████████████████     92%   Team K             │ │
│  │     🟢 │███████████████████       82%   Team A             │ │
│  │     🟢 │██████████████████        80%   Team D             │ │
│  │     🟢 │█████████████████         75%   Team G             │ │
│  │     🟢 │████████████████          72%   Team I             │ │
│  │     🟢 │███████████████           68%   Team J             │ │
│  │     🟢 │██████████████            65%   Team L             │ │
│  │     🟢 │████████████              60%   Team E             │ │
│  │     🟢 │███████████               55%   Team F             │ │
│  │     0% └───────────────────────────────────────           │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  团队详细负载                                                      │
│  [搜索...] [筛选: 全部▾] [排序: 利用率▾]                          │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Team C - 测试团队                                     🔴 107%││
│  │ ─────────────────────────────────────────────────────────  ││
│  │ 容量: 150 SP │ 已分配: 160 SP │ 超出: -10 SP               ││
│  │ Feature: 8个 │ 平均: 20 SP/个                              ││
│  │                                                             ││
│  │ Feature列表: (可拖拽调整)                                   ││
│  │   [F-001] 移动端登录优化 (25 SP) [→ 迁移]                  ││
│  │   [F-003] 支付流程改进 (22 SP) [→ 迁移]                    ││
│  │   [F-007] 性能监控 (20 SP) [→ 迁移]                        ││
│  │   [F-012] 数据统计 (18 SP)                                 ││
│  │   [F-015] UI组件库 (18 SP)                                 ││
│  │   ... 更多(3个)                                             ││
│  │                                                             ││
│  │ 💡 建议: 迁移2-3个Feature到Team E或Team F                  ││
│  └────────────────────────────────────────────────────────────┘││
│                                                                   │
│  │ Team F - DevOps团队                                   🟢 55%││
│  │ ─────────────────────────────────────────────────────────  ││
│  │ 容量: 200 SP │ 已分配: 110 SP │ 可用: 90 SP                ││
│  │ Feature: 5个 │ 平均: 22 SP/个                              ││
│  │                                                             ││
│  │ Feature列表:                                                ││
│  │   [F-020] CI/CD优化 (30 SP)                                ││
│  │   [F-024] 容器化改造 (28 SP)                               ││
│  │   [F-030] 监控告警 (22 SP)                                 ││
│  │   [F-035] 日志系统 (20 SP)                                 ││
│  │   [F-040] 备份方案 (10 SP)                                 ││
│  │                                                             ││
│  │ 💡 建议: 可承接2-3个Feature，推荐测试或性能相关              ││
│  │ [+ 从Team C接收Feature]                                    ││
│  └────────────────────────────────────────────────────────────┘││
│                                                                   │
│  [显示更多团队...]                                                │
└───────────────────────────────────────────────────────────────────┘

智能平衡对话框
┌────────────────────────────────────────────────────────────────┐
│  智能负载平衡                                          [×]       │
├────────────────────────────────────────────────────────────────┤
│                                                                  │
│  平衡策略:                                                        │
│    ○ 保守平衡 (目标利用率: 70-80%)                              │
│    ● 标准平衡 (目标利用率: 75-85%)  [推荐]                      │
│    ○ 激进平衡 (目标利用率: 80-90%)                              │
│                                                                  │
│  约束条件:                                                        │
│    ☑ 保持Feature完整性（不拆分Feature）                         │
│    ☑ 考虑团队技能匹配（优先分配匹配的Feature）                  │
│    ☑ 最小化Feature迁移数量                                      │
│    ☐ 锁定已开始的Feature                                        │
│                                                                  │
│  预览调整:                                                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 调整建议（5项）:                                           │  │
│  │                                                            │  │
│  │ 1. [F-001] 移动端登录优化                                 │  │
│  │    Team C (107%) → Team E (60%)  [-25 SP]                │  │
│  │    影响: Team C降至82%，Team E升至73%                     │  │
│  │                                                            │  │
│  │ 2. [F-003] 支付流程改进                                   │  │
│  │    Team C (82%) → Team F (55%)  [-22 SP]                 │  │
│  │    影响: Team C降至68%，Team F升至66%                     │  │
│  │                                                            │  │
│  │ ... 更多调整(3项)                                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  平衡效果:                                                        │
│    当前均衡度: 78分 → 预期: 92分 (提升18%)                       │
│    标准差: 12% → 预期: 7%                                       │
│    超载团队: 2个 → 预期: 0个                                    │
│    平均利用率: 77% → 预期: 79%                                  │
│                                                                  │
│  [取消]                              [预览详情]  [应用调整]      │
└────────────────────────────────────────────────────────────────┘
```

---

## 数据字段设计

```typescript
interface LoadBalance {
  piId: string;
  metrics: {
    balanceScore: number;          // 均衡度分数(0-100)
    standardDeviation: number;     // 标准差
    avgUtilization: number;        // 平均利用率
    healthyTeams: number;          // 健康团队数
    warningTeams: number;          // 警告团队数
    overloadTeams: number;         // 超载团队数
  };
  teams: TeamLoad[];
  suggestions: BalanceSuggestion[];
}

interface TeamLoad {
  teamId: string;
  teamName: string;
  capacity: number;                // 容量
  allocated: number;               // 已分配
  available: number;               // 可用
  utilization: number;             // 利用率
  status: 'healthy' | 'warning' | 'overload';
  features: FeatureAllocation[];
  skills: string[];                // 团队技能
}

interface FeatureAllocation {
  featureId: string;
  featureName: string;
  storyPoints: number;
  priority: string;
  skills: string[];                // 所需技能
  dependencies: string[];          // 依赖
  status: string;
  canMove: boolean;                // 是否可迁移
}

interface BalanceSuggestion {
  type: 'move' | 'split' | 'merge';
  featureId: string;
  featureName: string;
  fromTeam: string;
  toTeam: string;
  storyPoints: number;
  reason: string;
  impact: {
    fromUtilization: number;       // 源团队利用率变化
    toUtilization: number;         // 目标团队利用率变化
    balanceScoreChange: number;    // 均衡度变化
  };
  priority: 'high' | 'medium' | 'low';
}
```

---

## API接口设计

### 1. 获取负载均衡数据

```typescript
GET /api/pi/{piId}/load-balance

Response: {
  code: 200,
  data: {
    piId: "PI-2024-2",
    metrics: {
      balanceScore: 78,
      standardDeviation: 12,
      avgUtilization: 77,
      healthyTeams: 7,
      warningTeams: 3,
      overloadTeams: 2
    },
    teams: [...],
    suggestions: [...]
  }
}
```

### 2. 智能平衡计算

```typescript
POST /api/pi/{piId}/load-balance/calculate

Request: {
  strategy: 'conservative' | 'standard' | 'aggressive',
  constraints: {
    keepFeatureIntegrity: true,
    considerSkills: true,
    minimizeMoves: true,
    lockStartedFeatures: false
  }
}

Response: {
  code: 200,
  data: {
    suggestions: [...],
    expectedMetrics: {
      balanceScore: 92,
      standardDeviation: 7,
      avgUtilization: 79
    }
  }
}
```

### 3. 应用平衡调整

```typescript
POST /api/pi/{piId}/load-balance/apply

Request: {
  adjustments: [
    {
      featureId: "F-001",
      fromTeam: "TEAM-C",
      toTeam: "TEAM-E"
    }
  ]
}

Response: {
  code: 200,
  message: "负载平衡调整成功",
  data: {
    successCount: 5,
    failCount: 0
  }
}
```

---

## 交互设计

### 平衡度计算

```typescript
function calculateBalanceScore(teams: TeamLoad[]): number {
  // 1. 计算标准差
  const utilizations = teams.map(t => t.utilization);
  const avg = utilizations.reduce((a, b) => a + b) / teams.length;
  const variance = utilizations.reduce((sum, u) => sum + Math.pow(u - avg, 2), 0) / teams.length;
  const stdDev = Math.sqrt(variance);
  
  // 2. 标准差越小，均衡度越高
  const balanceScore = Math.max(0, 100 - stdDev * 5);
  
  // 3. 惩罚超载团队
  const overloadPenalty = teams.filter(t => t.utilization > 95).length * 10;
  
  return Math.max(0, balanceScore - overloadPenalty);
}
```

### 智能平衡算法

```typescript
function smartBalance(teams: TeamLoad[], strategy: string): BalanceSuggestion[] {
  const suggestions: BalanceSuggestion[] = [];
  
  // 1. 找出超载和空闲团队
  const overloadTeams = teams.filter(t => t.utilization > 85).sort((a, b) => b.utilization - a.utilization);
  const idleTeams = teams.filter(t => t.utilization < 75).sort((a, b) => a.utilization - b.utilization);
  
  // 2. 对每个超载团队
  for (const overloadTeam of overloadTeams) {
    // 找出可移动的Feature
    const movableFeatures = overloadTeam.features
      .filter(f => f.canMove)
      .sort((a, b) => b.storyPoints - a.storyPoints);
    
    for (const feature of movableFeatures) {
      // 找最合适的目标团队
      const targetTeam = findBestTargetTeam(feature, idleTeams);
      
      if (targetTeam && wouldImproveBalance(feature, overloadTeam, targetTeam)) {
        suggestions.push({
          type: 'move',
          featureId: feature.featureId,
          featureName: feature.featureName,
          fromTeam: overloadTeam.teamId,
          toTeam: targetTeam.teamId,
          storyPoints: feature.storyPoints,
          reason: `Team ${overloadTeam.teamName}超载，Team ${targetTeam.teamName}有余量`,
          impact: calculateImpact(feature, overloadTeam, targetTeam),
          priority: 'high'
        });
      }
    }
  }
  
  return suggestions;
}
```

---

## 业务规则

### 平衡策略

- **保守**: 目标利用率70-80%，保留较大缓冲
- **标准**: 目标利用率75-85%，推荐策略
- **激进**: 目标利用率80-90%，最大化利用

### Feature迁移规则

1. 优先迁移未开始的Feature
2. 考虑团队技能匹配
3. 保持Feature完整性
4. 最小化迁移次数
5. 尊重Feature依赖关系

---

**设计完成日期**: 2026-01-17  
**设计人员**: Auto-RD平台设计组
