# C3-F09: 依赖识别

> **功能编号**: C3-F09  
> **功能名称**: 依赖识别  
> **所属模块**: C3-规划协调 > 依赖管理  
> **优先级**: P0（核心功能）

## 功能概述

依赖识别功能用于在PI Planning和Sprint规划阶段，识别和记录Feature、MR、Task之间的依赖关系。系统支持手动添加依赖、智能识别依赖、依赖类型分类，并提供依赖影响分析。

### 业务价值
- 风险识别：提前识别依赖风险
- 协调计划：为跨团队协调提供依据
- 优先级调整：根据依赖关系调整优先级
- 交付保障：确保依赖项按顺序交付

## 页面布局

```
┌──────────────────────────────────────────────────────────┐
│  依赖识别 - PI 2024.2                                      │
├──────────────────────────────────────────────────────────┤
│  [←返回] [智能识别] [批量导入] [导出]                      │
├──────────────────────────────────────────────────────────┤
│                                                            │
│  依赖总览                                                  │
│  ┌────────────────────────────────────────────────────┐  │
│  │ 总依赖: 48 │ 内部: 32 │ 外部: 16 │ 风险: 5      │  │
│  └────────────────────────────────────────────────────┘  │
│                                                            │
│  依赖列表                                                  │
│  [搜索...] [筛选: 全部▾] [类型: 全部▾] [+ 添加依赖]      │
│  ┌────────────────────────────────────────────────────┐  │
│  │ [D-001] Feature依赖                      🔴 高风险 │  │
│  │ ───────────────────────────────────────────────── │  │
│  │ 源: [F-005] 用户系统改造 (Team A)                 │  │
│  │ 目标: [F-012] 权限系统升级 (Team B)               │  │
│  │ 类型: 前置依赖 (Finish-to-Start)                 │  │
│  │ 状态: 🟡 待解决 │ 负责人: 张三                    │  │
│  │ 风险: F-005延期2周，影响F-012开始时间             │  │
│  │ [详情] [编辑] [解决]                              │  │
│  └────────────────────────────────────────────────────┘  │
│                                                            │
│  │ [D-002] MR依赖                           🟡 中风险 │  │
│  │ 源: [MR-015] API接口设计 (Team C)                 │  │
│  │ 目标: [MR-023] 前端集成 (Team A)                  │  │
│  │ 类型: 接口依赖                                     │  │
│  │ 状态: 🟢 已协调 │ 计划交付: 2024-05-01           │  │
│  └────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘

添加依赖对话框
┌──────────────────────────────────────────────────────────┐
│  添加依赖关系                                    [×]       │
├──────────────────────────────────────────────────────────┤
│  依赖源(被依赖项):                                         │
│  [选择Feature/MR/Task...▾]                                │
│  已选: [F-005] 用户系统改造                               │
│                                                            │
│  依赖目标(依赖项):                                         │
│  [选择Feature/MR/Task...▾]                                │
│  已选: [F-012] 权限系统升级                               │
│                                                            │
│  依赖类型:                                                 │
│  ○ Finish-to-Start (F-005完成后F-012才能开始)            │
│  ○ Start-to-Start (F-005开始时F-012可以开始)             │
│  ○ Finish-to-Finish (F-005完成时F-012也要完成)           │
│  ○ Start-to-Finish (F-005开始后F-012才能完成)            │
│                                                            │
│  依赖强度:                                                 │
│  ● 强依赖(必须) ○ 弱依赖(建议)                           │
│                                                            │
│  说明: [可选，描述依赖原因...]                            │
│                                                            │
│  负责人: [选择...▾]                                        │
│                                                            │
│  [取消]                                          [确定]    │
└──────────────────────────────────────────────────────────┘
```

## 数据字段

```typescript
interface Dependency {
  id: string;
  type: 'FS' | 'SS' | 'FF' | 'SF';  // 依赖类型
  sourceId: string;                   // 源ID
  sourceType: 'feature' | 'mr' | 'task';
  sourceName: string;
  sourceTeam: string;
  targetId: string;                   // 目标ID
  targetType: 'feature' | 'mr' | 'task';
  targetName: string;
  targetTeam: string;
  strength: 'strong' | 'weak';        // 依赖强度
  status: 'identified' | 'coordinating' | 'resolved' | 'blocked';
  riskLevel: 'high' | 'medium' | 'low';
  owner: string;                      // 负责人
  description: string;
  createdAt: Date;
}
```

## API接口

```typescript
// 获取依赖列表
GET /api/pi/{piId}/dependencies

// 添加依赖
POST /api/dependencies
Request: {
  sourceId: "F-005",
  sourceType: "feature",
  targetId: "F-012",
  targetType: "feature",
  type: "FS",
  strength: "strong"
}

// 智能识别依赖
POST /api/pi/{piId}/dependencies/auto-detect
Response: { dependencies: [...] }
```

**设计完成日期**: 2026-01-17
