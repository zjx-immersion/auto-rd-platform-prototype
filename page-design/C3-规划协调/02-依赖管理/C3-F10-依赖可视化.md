# C3-F10: 依赖可视化

> **功能编号**: C3-F10  
> **功能名称**: 依赖可视化  
> **所属模块**: C3-规划协调 > 依赖管理  
> **优先级**: P0（核心功能）

## 功能概述

依赖可视化功能通过图形化的方式展示Feature/MR/Task之间的依赖关系，包括依赖关系图、依赖矩阵、时间线视图等多种可视化方式，帮助团队直观理解依赖关系，识别关键路径和风险点。

### 业务价值
- 直观展示：可视化依赖关系，一目了然
- 关键路径：识别关键路径，优先保障
- 风险预警：识别依赖风险，提前应对
- 协调沟通：为团队协调提供可视化工具

## 页面布局

```
┌──────────────────────────────────────────────────────────┐
│  依赖可视化 - PI 2024.2                                    │
├──────────────────────────────────────────────────────────┤
│  视图: [关系图] [矩阵] [时间线] [甘特图]                  │
│  [←返回] [缩放] [筛选] [导出图片]                         │
├──────────────────────────────────────────────────────────┤
│                                                            │
│  依赖关系图视图                                            │
│  ┌────────────────────────────────────────────────────┐  │
│  │                                                     │  │
│  │          ┌────────────┐                            │  │
│  │          │  F-001     │                            │  │
│  │          │ 用户系统   │                            │  │
│  │          └─────┬──────┘                            │  │
│  │                │                                    │  │
│  │         FS(强依赖)                                  │  │
│  │                ↓                                    │  │
│  │          ┌─────────────┐      ┌─────────────┐     │  │
│  │          │  F-005      │──SS→ │  F-008      │     │  │
│  │          │ 权限系统    │      │ 审计日志    │     │  │
│  │          └─────┬───────┘      └─────────────┘     │  │
│  │                │                                    │  │
│  │         FS(强依赖)                                  │  │
│  │                ↓                                    │  │
│  │          ┌─────────────┐                           │  │
│  │          │  F-012      │                           │  │
│  │          │ 数据迁移    │                           │  │
│  │          └─────────────┘                           │  │
│  │                                                     │  │
│  │  图例:                                              │  │
│  │    ─FS→ 前置依赖  ─SS→ 同步开始                   │  │
│  │    🔴 高风险  🟡 中风险  🟢 低风险                │  │
│  └────────────────────────────────────────────────────┘  │
│                                                            │
│  依赖矩阵视图                                              │
│  ┌────────────────────────────────────────────────────┐  │
│  │       │F-001│F-005│F-008│F-012│F-015│...           │  │
│  │ ──────┼─────┼─────┼─────┼─────┼─────┼──          │  │
│  │ F-001 │  -  │ FS  │  -  │  -  │  -  │            │  │
│  │ F-005 │  ↑  │  -  │ SS  │ FS  │  -  │            │  │
│  │ F-008 │  -  │  ↑  │  -  │  -  │ FF  │            │  │
│  │ F-012 │  -  │  ↑  │  -  │  -  │  -  │            │  │
│  │ F-015 │  -  │  -  │  ↑  │  -  │  -  │            │  │
│  │ ...   │     │     │     │     │     │            │  │
│  └────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

## 数据字段

```typescript
interface DependencyGraph {
  nodes: GraphNode[];
  edges: GraphEdge[];
  criticalPath: string[];      // 关键路径
  riskyDependencies: string[]; // 风险依赖
}

interface GraphNode {
  id: string;
  name: string;
  type: 'feature' | 'mr' | 'task';
  team: string;
  status: string;
  startDate: Date;
  endDate: Date;
}

interface GraphEdge {
  source: string;
  target: string;
  type: 'FS' | 'SS' | 'FF' | 'SF';
  strength: 'strong' | 'weak';
  riskLevel: 'high' | 'medium' | 'low';
}
```

## API接口

```typescript
// 获取依赖图数据
GET /api/pi/{piId}/dependencies/graph
Response: {
  nodes: [...],
  edges: [...],
  criticalPath: ["F-001", "F-005", "F-012"],
  riskyDependencies: ["D-003", "D-007"]
}

// 计算关键路径
POST /api/dependencies/critical-path
Response: { path: [...], duration: 180 }
```

**设计完成日期**: 2026-01-17
